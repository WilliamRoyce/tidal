# Pending GitHub Issues

These issues were identified during E-L velocity form verification (2026-02-27).
Create with `gh issue create` when the CLI is available.

---

## RESOLVED: Energy measurement missing sqrt|g| for curved coordinates (was Issue 1)

**Status:** Fixed in commit 01fa20c

Added `volume_element` (sqrt|det(g_spatial)|) computation to the Wolfram pipeline, conditional injection
into JSON canonical structure, and volume-weighted energy integration in \_energy.py.

Results after fix:

- polar_kg: 43% -> 0.9% error
- sphere_kg: 66% -> 4.4e-9 error
- Cartesian examples: unchanged (no regression)

---

## RESOLVED: Rename data.momenta attribute to data.velocities (was Issue 2)

**Status:** Fixed in commit 01fa20c

Renamed SimulationData.momenta -> SimulationData.velocities across all measurement modules,
CLI, writer, and tests. Backward-compatible metadata reading (accepts both keys).

---

## RESOLVED: Background field Hamiltonian substitution (was Issue 2)

**Status:** Fixed in commit 21548b8

The `_wls_vector_background_substitution` function only generated indexed-form rules (`vbdB[{i, -chart}] -> value`) but after `DecomposeScalarExpression`, the expression contained component-function form (`vbdBi[t, x, y]`). Fix adds function-form rules (`vbdBi[__] -> value`).

After re-derivation, vector_background energy conservation: PASS (max |dE/E| = 2.78e-06).

## RESOLVED: proca_background energy conservation (was Issue 3)

**Status:** Resolution issue, not a bug

At 16x16 grid: max |dE/E| = 5.43e-03 (FAIL)
At 24x24 grid with rtol=1e-5: max |dE/E| = 4.24e-04 (PASS)

Spatial resolution noise, not a physics bug. The position-dependent Lorentzian coupling `g0/(1 + r^2/R^2)` requires sufficient grid resolution.

---

## RESOLVED: Energy measurement: support mixed\_\* Hamiltonian operators

**Status:** Fixed (Python measurement layer)

Added general `mixed_*` operator evaluation to `_energy.py`. Decomposition strategy:

- Parse `mixed_T_S1_S2_...` notation (time order + spatial orders per axis)
- Resolve time derivatives: order 0 → field, order 1 → velocity, order 2 → acceleration from EOM RHS
- For constraint fields: differentiate constraint equation (velocity sources → acceleration, field sources → velocity/acceleration)
- Apply spatial gradients sequentially after time resolution
- Wired into `_evaluate_hamiltonian_factor` dispatch for seamless integration

Also added `mixed_*` to operator validation in `json_loader.py`.

**Complementary Wolfram IBP optimization** (not yet implemented): Integration by parts on the Lagrangian before the Legendre transform could reduce mixed operators at the source. The Python handler is the primary mechanism; IBP is an optimization for cleaner JSON

---

## OPEN: Pre-decomposition reduction for faster GW derivation

**Status:** Open (optimization)
**Priority:** Low
**Labels:** enhancement, performance

Benchmark (2026-02-28) of GW plane-wave derivation (18 min total):

- xPert + VarD: 0.4 sec (0%) -- abstract symbolic phases are fast
- DecomposeToComponents: 469 sec (42%) -- converts abstract EOM to 10 component equations
- DecomposeScalarExpression (canonical): 634 sec (57%) -- Hamiltonian decomposition

**Pre-linearization reduction is NOT worth it** since xPert+VarD take <1 sec.

**Actual optimization:** Per-component early exit in `DecomposeToComponents`. After `ToBasis` but before expensive `Expand`, zero transverse Derivative patterns. If the component is identically zero, skip remaining steps. For GW with plane-wave reduction, 8 of 10 components would short-circuit, potentially saving ~80% of decomposition time.

Requires modifying `ComponentDecompose.wl` (`ExtractTensorComponent`) and `DecomposeScalarExpression`. Both are shared infrastructure requiring careful testing.

See `memory/pre-linearization-benchmark.md` for full analysis.

---

## RESOLVED: SuperLU memory failure on moderate grids — nnz-based GMRES fallback

**Status:** Resolved
**Labels:** bug, solver, performance

**Problem:** IDA's sparse tier (2K < N ≤ 200K state variables) used SuperLU_MT for direct
Jacobian factorization. On moderate grids, SuperLU's LU fill-in (20–50× nnz) exhausted
memory and crashed:

- 32×32 × 12 slots = 12,288 unknowns → nnz ~172K → SuperLU crash
- 16×16 × 12 slots = 3,072 unknowns → nnz ~43K → SuperLU worked

**Workaround (now removed):** `examples/massive_gravity/run.sh` was reduced to 16×16 grid
(commit 6bac7f5). Restored to 32×32 as part of this fix.

**Fix:** Added `SUPERLU_NNZ_LIMIT = 100_000` to `tidal/solver/_types.py`. In
`configure_linear_solver()` (`tidal/solver/_setup.py`), after building the sparsity pattern,
if `pattern.nnz > SUPERLU_NNZ_LIMIT` the sparse tier falls back to FD GMRES (identical to
the N > 200K path, no `jactimes` callback, safe with `IDACalcIC`) with a `UserWarning`.
Systems below the limit continue to use SuperLU unchanged.

Note: The IDACalcIC regression in commit 7f3df3e was specific to the analytical `jactimes`
callback, not FD GMRES. The FD GMRES path (no `jactimes`) is unaffected.

**Relevant code:**

- `tidal/solver/_types.py` — `SUPERLU_NNZ_LIMIT = 100_000` added
- `tidal/solver/_setup.py:configure_linear_solver()` — nnz check + GMRES fallback
- `tests/test_analytical_jacobian.py:TestSuperLUNnzFallback` — 3 new tests

---

## OPEN: Energy measurement incorrect with position-dependent mass/potential terms

**Status:** Open (bug)
**Priority:** Medium
**Labels:** bug, measurement
**Identified:** 2026-03-09 during Gertsenshtein Phase F2 planning

**Problem**: `tidal measure --what energy` does not correctly account for spatially-varying
mass or potential terms. The Hamiltonian density computation in `_energy.py` assumes
translation-invariant coefficients; it evaluates coefficients at a single point (or as
constants) rather than integrating the position-dependent potential energy density over
the grid.

**Affected configurations:**

- Position-dependent mass terms: `m²(z) φ²` — common in plasma gradient / MSW crossing setups
- Localized Gertsenshtein coupling: effective mass term `B₀²κ²` on h_7 becomes position-dependent with localized B₀(z)
- Potential wells: `examples/scalar_potential_well/` (already documented in that example)

**Does NOT affect:**

- Conversion probability measurement (`--what conversion`) — measures amplitude ratios
- Conservation measurement (`--what conservation`) — measures relative change, not absolute energy

**Workaround**: Use `--what conversion` as primary validation metric for scattering setups.
For energy checks, use `--what conservation` (tracks relative ΔE/E rather than absolute value).

**Fix direction**: In `tidal/measurement/_energy.py`, the potential energy term should
evaluate `coefficient(x) * field(x)² ` as a grid array and integrate over the spatial grid,
analogous to how kinetic terms are handled. The `CoefficientEvaluator.resolve()` already
supports position-dependent coefficients — the fix is to use the grid-resolved array
(L2 cache) rather than a scalar when computing Hamiltonian contributions.

**Relevant files:**

- `tidal/measurement/_energy.py` — Hamiltonian density computation
- `tidal/measurement/_eval_utils.py` — coefficient evaluation utilities
- `tidal/solver/coefficients.py` — `CoefficientEvaluator` L2 spatial cache
- `docs/background_fields.md` — documents the limitation

---

## OPEN: Ghost detection for `tidal validate --stability`

**Status:** Open (future enhancement — requires Wolfram pipeline changes)
**Priority:** Low
**Labels:** enhancement, validate
**Identified:** 2026-03-09 during supervisor website review

**Problem**: Ghost detection from `canonical.hamiltonian_terms` in `tidal validate --stability`
is fundamentally unreliable and was removed. Ghost detection requires the physical kinetic
matrix K[i,j] after constraint reduction and gauge fixing — not the naive Hamiltonian
kinetic coefficients stored in the JSON.

**Why naive H kinetic coefficients fail:**

1. **Pure gauge theories (linearized GR)**: In Fierz-Pauli massive gravity, all dynamical
   metric components (h_3, h_4, h_5) have negative Hamiltonian kinetic coefficients (−0.5,
   −1.5, −0.5) even though FP is ghost-free. FP ghost-freeness comes from the mass term
   cancelling the Boulware-Deser ghost, not from kinetic positivity.
2. **Mixed theories (matter + gauge)**: Scalar matter fields have positive kinetic; metric
   perturbations have negative kinetic — both present causes false positives for all metric
   fields. The spacetime metric signature alone does not resolve this (the sign depends on
   gauge structure and ADM decomposition, not just the spacetime metric).

**Workaround**: Use dedicated tools such as xAct/Mathematica ghost-checking routines, or
manually verify ghost-freeness in a unitary/TT gauge.

**Future implementation paths:**

_Option A (Wolfram change) — Export `"kinetic_matrix"` and `"kinetic_matrix_symbolic"`_:
Add to the `"canonical"` JSON section the kinetic matrix K[i,j] computed from the quadratic
Lagrangian (coefficient of `∂_t φ^i ∂_t φ^j` in L, before E-L reduction). Check eigenvalues
of K — a negative eigenvalue indicates a ghost. Limitation: for gauge theories K may still
be indefinite due to gauge modes even in ghost-free theories; needs field-type information.

_Option B (Wolfram change) — Field type annotation_:
Add `"kind": "scalar" | "vector_spatial" | "vector_temporal" | "metric_perturbation"` to
each field in the JSON. The Python ghost check can then apply the correct expected-sign logic
per field class (e.g. skip metric perturbations, which are always ambiguous without gauge
fixing).

_Option C — Restrict ghost check to scalar sector only_:
Simplest if Option B is implemented — only check fields with `"kind": "scalar"` for
negative kinetic coefficients.

**Relevant code:**

- `tidal/cli/_validate.py` — `_run_stability_checks()` docstring documents the limitation
- `tidal/wolfram/ExportJSON.wl` — where kinetic matrix or field-type export would be added
- References: Boulware & Deser (1973) PRD 6:3368; Hassan & Rosen (2012) arXiv:1106.3344

---

## OPEN: Sponge-layer / absorbing boundary conditions

**Status:** Open (enhancement)
**Priority:** Medium
**Labels:** enhancement, solver, boundary-conditions
**Identified:** 2026-03-09 during Gertsenshtein Phase F2/F3 planning

**Problem**: TIDAL supports periodic, Neumann, Dirichlet, and Robin BCs only. For
open-domain scattering setups — wavepacket entering a localized coupling region and
exiting — outgoing waves either wrap around (periodic) or reflect off boundaries
(Dirichlet/Neumann), contaminating the conversion measurement.

**Affected use cases:**

- Gertsenshtein Phase F2: localized B₀(z) scattering (wavepacket enters magnetized region)
- Gertsenshtein Phase F3: magnetar/FRB (inward GW from large r, outgoing EM)
- Any finite-region scattering problem

**Workaround**: Use a very large domain with the coupling region in the center; terminate
simulation before outgoing waves reach boundaries. This is the pattern used in
`examples/coupled_scattering/`.

**Proposed implementation (Phase 1 — sponge layer)**:

- Add a spatially-varying dissipation term `-σ(x) ∂_t f` in boundary regions
- σ(x) ramps from 0 (inside domain) to σ_max (near boundaries) over a layer width
- Can be implemented via `[[background_fields]]` with coordinate-dependent σ(x)
  plus a new `first_derivative_t` coupling term in the Lagrangian
- Validate: reflected power < 1% at optimal σ_max

**Proposed implementation (Phase 2 — proper PML)**:

- Perfectly Matched Layer via complex coordinate stretching
- Requires split-field formulation; larger code change
- Industry standard (MEEP, FEniCS, Devito)

**Relevant files:**

- `tidal/solver/operators.py` (lines 40-182) — `SideBCSpec`, `_pad_axis()`, ghost-cell BCs
- `tidal/solver/grid.py` — `GridInfo` BC metadata
- `tidal/cli/_simulate.py` (lines 213-295) — CLI BC argument parsing

---

## OPEN: Plasma detuning (Gertsenshtein Phase F1) requires gauge-invariant mass mechanism

**Status:** Open (physics limitation)
**Priority:** Medium
**Labels:** enhancement, gertsenshtein, physics
**Identified:** 2026-03-09 during Phase F1 implementation

**Problem**: Adding a Proca-type photon mass `- omegaP2/2 * a[-mu] eta[mu, nu] a[-nu]` to the
full Einstein-Maxwell Lagrangian for the Gertsenshtein effect generates position-dependent
coefficients `B₀²κ²omegaP2 z²` on the graviton h_7 equations. This breaks the uniform plasma
model and is incompatible with periodic BCs.

**Root cause**: In TIDAL's xPert pipeline, the standalone perturbation field `a` (the photon
perturbation) is treated by xPert as a tensor with non-zero background value (equal to the
background gauge potential `Abar_y = -B₀z`). When the plasma mass `(omegaP2/2)|a|²` is expanded
to second order in the action `∫ √(-g) × L`, the term `(ε/2)Tr(h) × (omegaP2/2)|Abar|²` contributes
at O(ε²), giving position-dependent graviton mass `B₀²κ²omegaP2 z²`. Any gauge representation of
a transverse uniform B-field has `|Abar|² ∝ z²`, so no gauge choice avoids this.

**Why an effective scalar theory won't do**: The physical mixing modes (h\_+, a_y) reduce to scalars
in 1+1D — no different from the existing `coupled_scalars` example. The physics value of the plasma
example requires proper field types (tensor graviton, vector photon).

**What would be needed**:

_Option A — Stueckelberg mass_: Replace the Proca mass with a gauge-invariant Stueckelberg mechanism.
The Stueckelberg scalar `φ` absorbs the gauge degree of freedom; the mass term becomes
`-(omegaP2/2)(a_μ + ∂_μφ)²`. In unitary gauge (φ=0), this reduces to the Proca mass but without
the background-field coupling artifact. Requires adding a Stueckelberg scalar field to the theory.

_Option B — EOM-level medium_: Implement plasma as a medium term directly in the equations of
motion (adding `-omegaP2 * a_mu` to the photon EOM only, not derived from a Lagrangian term).
Would require a new `medium_terms` section in the JSON spec and CLI. Clean physics but bypasses
the Lagrangian derivation philosophy.

_Option C — Modified xPert pipeline_: In `_wls_matter_pert_truncation()`, explicitly zero the
background value of the perturbation field `gea` before xPert expansion, ensuring the plasma mass
is treated as purely O(ε²). This would require modifying the Wolfram pipeline to explicitly
declare `gea` as having zero background.

**Blocked by**: Needs investigation of whether Option C is feasible in xAct/xPert without
breaking other theories.

**Relevant code:**

- `tidal/cli/_derive.py:_wls_matter_pert_truncation()` (lines 847-883) — truncation logic
- `examples/gertsenshtein/theory.toml` — base Gertsenshtein (implemented)
- `docs/gertsenshtein.md` (Target 3) — physics description
- Raffelt & Stodolsky (1988, PRD 37:1237); Dandoy, Lella et al. (2024, arXiv:2406.17853)

---

## OPEN: Conversion measurement gives wrong results in curved coordinates (per-field energy ≠ physical energy)

**Status:** Open (investigation needed)
**Priority:** High
**Labels:** bug, measurement, curved-coordinates
**Identified:** 2026-03-11 during Gertsenshtein Phase F3a (spherical radial)

**Problem**: The conversion measurement `tidal measure --what conversion` uses per-field
Hamiltonian self-energy (kinetic + gradient + mass for each field separately) as the numerator
and denominator for P(t) = E_target(t) / E_source(0). In curved coordinates (e.g. spherical),
the per-field self-energy of component fields is dominated by coordinate artifacts, making the
conversion probability physically meaningless.

**Root cause**: In spherical coordinates, the component field h*{θθ} (= h_7 in TIDAL indexing)
relates to the physical gravitational wave strain ψ by h*{θθ} = r² × ψ. The self-energy of the
component field includes geometric factors:

- Kinetic: ½⟨(∂h_7/∂t)²⟩ = ½⟨r⁴(∂ψ/∂t)²⟩ — grows as r⁴ for outward-propagating wave
- Gradient: ½⟨(∂h_7/∂r)²⟩ — similarly grows geometrically

The Hamiltonian interaction energy includes Christoffel-connection terms (e.g. 2/r × ∂_r h
coupling) that exactly cancel this geometric growth, keeping the total Hamiltonian conserved.
However, the interaction energy mixes two physically distinct contributions:

1. **Actual graviton-photon coupling** (Gertsenshtein effect) — the physics we want to measure
2. **Geometric Christoffel self-coupling** — coordinate artifacts from component field → physical
   field mapping

There is no clean way to separate these from the Hamiltonian per-field decomposition alone.

**Observed numerics** (spherical dipolar, Bpeak=40, domain [5,80]):

- H*self(h_7) = 10274 (dominated by r² geometric amplification of h*{θθ})
- H_interaction = -10274 (Christoffel corrections cancel geometric self-energy)
- H_total ≈ 0 (correctly conserved)
- P_measured = E_self(a_2) / E_self(h_7, t=0) = 0.000025
- P_Boccaletti = sin²(κBpeak z0³/(4R²)) ≈ 0.147

The conversion measurement is off by ~6000× because it divides by the coordinate-inflated
h_7 self-energy rather than the physical graviton energy.

**Note**: The volume-weight fix (same session) ensures the per-field energies correctly include
the spatial volume element √|g_spatial| = r² for the energy integral. This is necessary but
not sufficient — the fundamental issue is that component field self-energy ≠ physical field
energy in curved coordinates.

**What works correctly**:

- Flat-space conversion measurements (component fields = physical fields)
- Total Hamiltonian conservation in curved coordinates
- Energy measurement per-field decomposition for flat spacetimes

**Investigation needed**:

1. **Physical field extraction**: Define "physical energy" via gauge-invariant quantities.
   For gravitons in TT gauge: E*phys = ½∫ r² [(∂ψ/∂t)² + (∂ψ/∂r)²] dr where ψ = h*{θθ}/r².
   For photons: similarly extract physical transverse EM field from component a_θ.
   Requires knowing the metric-to-physical-field mapping per field type.
2. **Flat-space proxy**: Use flat Minkowski metric with the same B-field profile
   (dipolar 1/z³) — component fields = physical fields, conversion measurement works.
   Validates the same Boccaletti formula without curved-coordinate complications.
   (Implementation: `examples/gertsenshtein/theory_dipolar.toml` — flat space proxy.)
3. **WKB conversion measurement**: In the WKB limit (k >> 1/r), the spherical wave equation
   for u = r×h reduces to the flat-space form. The conversion probability could be measured
   from the WKB-reduced variables u_grav and u_photon directly. Requires post-processing of
   the simulation output with the r-dependent field rescaling.

**Immediate workaround**: Use the flat-space dipolar theory (`theory_dipolar.toml`) for
Boccaletti validation. The spherical coordinate theory is correct for the PDE evolution
(the equations are mathematically right), but the conversion measurement needs a different
approach for curved metrics.

**Investigation direction — coordinate-independent per-field energy**:
The total Hamiltonian H is a scalar (coordinate-independent). The issue is that the
per-field decomposition H = Σ H*self(field_i) + H_interaction depends on which field
variables are used. For tensor fields in curved coordinates, component variables include
coordinate factors (e.g. h*{θθ} = r² ψ for the physical strain ψ). The self-energy of
the component variable is NOT the physical field energy.

A coordinate-independent per-field energy decomposition would require:

_Option 1 — Physical field extraction_:
Define physical (gauge-invariant) variables:

- Graviton: ψ = h\_{θθ}/r² (physical TT strain), energy density ∝ ⟨(∂ψ/∂t)²⟩ r²
- Photon: similarly, extract physical transverse EM field from component a_θ
- Requires a "physical_field_map" in the JSON spec: `{"h_7": "h_7 / x[]^2"}`
- The conversion measurement would use P = E_phys(a)/E_phys(h, t=0)

_Option 2 — Invariant norm from the field-space metric_:
For a multi-field theory with fields φ^I living on a field-space manifold with metric
G_IJ (from the kinetic terms in the Lagrangian), the gauge-invariant energy is:
E = ½ G_IJ ⟨∂_t φ^I ∂_t φ^J⟩ + ½ G_IJ ⟨∂_i φ^I ∂_i φ^J⟩
The field-space metric G_IJ diagonalizes the kinetic term and correctly weights each
component. This is already partially implemented in the canonical Hamiltonian evaluation
(which sums over hamiltonian_terms with their field-space coefficients), but the per-field
split uses `compute_field_energy` which ignores cross-kinetic terms.

_Option 3 — WKB variable transformation_:
In the WKB limit (k >> geometric potential terms), define u_i = √(g_spatial) × h_i^phys.
The u_i satisfy flat-space wave equations plus coupling. The self-energy of u_i IS the
physical field energy, invariant under coordinate changes. Requires post-processing the
simulation output with coordinate-dependent rescaling factors.

**Status of investigation**: Options 1 and 3 are most promising. Option 3 is simplest to
implement (post-processing only, no pipeline changes) but only valid in WKB limit.
Option 1 is general but requires extending the JSON spec and \_energy.py.

**Relevant files:**

- `tidal/measurement/_conversion.py` — conversion probability measurement
- `tidal/measurement/_energy.py` — per-field Hamiltonian energy decomposition
- `examples/data/gertsenshtein_radial.json` — spherical dipolar theory (derived)
- `examples/gertsenshtein/theory_dipolar.toml` — flat-space proxy (workaround)
- `docs/gertsenshtein.md` — physics documentation

---

## OPEN: Optimize canonical Lagrangian decomposition (24 min for flat Einstein-Maxwell)

**Status:** Open (performance)
**Priority:** Medium
**Labels:** performance, derivation, wolfram
**Identified:** 2026-03-12 during derivation performance profiling

**Problem**: The canonical Lagrangian decomposition via `DecomposeScalarExpression` is the
dominant cost in the derivation pipeline — 24 of 40 minutes for flat 4D Einstein-Maxwell
(`examples/gertsenshtein/theory.toml`). This phase decomposes 34 additive Lagrangian terms
independently via `DecomposeScalarExpression`, each going through the full
`SeparateFieldMetrics` → `ToBasis` → `TraceBasisDummy` → metric evaluation pipeline.

Previously (~10 min total) the canonical pipeline was skipped via a fast path; since commit
`58dcac4` the full Legendre transform always runs for correct coordinate-invariant energy
measurement.

**Profiling data** (flat 4D Einstein-Maxwell, 2026-03-12):

| Phase | Time | Notes |
|-------|------|-------|
| EOM h decomposition (6 comp) | ~14 min | 332 & 226 terms per comp |
| EOM a decomposition (4 comp) | ~1.5 min | 99 terms per comp |
| Linearization total | ~16 min | Includes xPert + gauge |
| **Canonical Lagrangian decomp** | **~24 min** | **34 additive terms, DOMINANT** |
| Legendre + parse | <1s | Trivial |

The EOM pass decomposes the **equations of motion** (`VarD[L]`) — a fundamentally different
algebraic object from the Lagrangian. Caching the EOM decomposition result for reuse in the
canonical path is not possible. Wolfram kernel state (Christoffels, metric DownValues,
background field DownValues) IS already shared between passes via `Share[]`.

**Investigation directions**:

_Option A — Shared sub-expression caching in `DecomposeScalarExpression`_:
Many of the 34 Lagrangian terms share the same tensor structure (field heads, index patterns,
metric contractions). The `ToBasis` + `TraceBasisDummy` pipeline repeats identical index
summation work across similar terms. A structural hash of each term's tensor skeleton could
identify groups that share the same decomposition, with only the scalar prefactor differing.
Decompose once per group, then multiply by the per-term coefficient.

Complexity: High — requires classifying abstract tensor terms by structure in Wolfram, which
means pattern-matching on xAct's internal representation. Potential 2-5x speedup for theories
with repetitive Lagrangian structure (Einstein-Maxwell: 34 terms from ~10 distinct structures).

_Option B — Combine EOM and canonical decomposition into a single pass_:
Instead of decomposing the EOM and Lagrangian separately, decompose the Lagrangian FIRST,
then derive the EOM from the component-form Lagrangian using `D[lagComp, field_comp]`.
This eliminates the abstract-index `VarD` + `DecomposeToComponents` pass entirely.

Trade-off: VarD at the abstract level is more robust (handles gauge symmetry, integration
by parts automatically). Component-level variation would need explicit IBP handling and
careful tracking of boundary terms. May introduce subtle bugs for theories with non-trivial
gauge structure.

Complexity: High — fundamental pipeline restructuring. Potential 1.6x speedup (eliminate
16 min of EOM decomposition, canonical becomes the only decomposition).

_Option C — Incremental `Expand[]` in `BatchedTraceBasisDummyWithMetric`_:
Profiling shows `Expand[]` inside `BatchedTraceBasisDummyWithMetric` is the dominant
sub-operation. Currently `Expand[]` is called on the full summed result after
`TraceBasisDummy`. An alternative: `ExpandAll` only on newly-created tensor contractions,
or `Simplify` with a targeted complexity limit.

Complexity: Medium — requires understanding which sub-expressions in the output of
`TraceBasisDummy` benefit from expansion and which create unnecessary intermediate growth.

_Option D — Wolfram `FunctionCompile` for metric evaluation_:
The metric evaluation step (`EvaluateMetricComponents`) dispatches on component indices
to substitute metric values. For diagonal metrics (most cases), this is a pure lookup:
`g_{ii} → value_i`, `g_{ij} → 0` for i≠j. Wolfram's `FunctionCompile` could bypass the
pattern matcher entirely for this step, replacing symbolic rule application with compiled
dispatch. Low risk, focused optimization.

Complexity: Low-Medium — need to verify FunctionCompile handles the output format of
TraceBasisDummy correctly. Potential 1.5-2x speedup on the metric evaluation sub-step.

_Option E — Pre-decompose Lagrangian before xPert expansion_:
For linearization theories, the Lagrangian is written in abstract-index form, expanded to
2nd order by xPert, then decomposed. If the NON-expanded Lagrangian were decomposed to
components first, the 2nd-order expansion could be done at the component level (faster,
since component expressions are scalar). However, xPert's `Perturbation[]` requires
abstract tensors — it cannot operate on component expressions.

Complexity: Very high — would require reimplementing perturbation theory at the component
level. Not recommended unless all other options prove insufficient.

**Mitigation already in place**: Derivation caching (`--force-derive` to bypass) means the
40-minute cost is paid only once per theory. Subsequent runs with unchanged TOML hit the
SHA-256 cache and return instantly. This makes the optimization lower-priority for most
workflows but still important for theory development iteration.

**Relevant code:**

- `tidal/cli/_derive.py:_wls_canonical_phase_a()` / `_wls_canonical_phase_b()` — canonical decomposition + Hamiltonian
- `tidal/wolfram/ComponentDecompose.wl:DecomposeScalarExpression()` (lines 880-973) — scalar decomposition pipeline
- `tidal/wolfram/ComponentDecompose.wl:BatchedTraceBasisDummyWithMetric()` (lines 409-498) — batched fused trace
- `tidal/wolfram/ExportJSON.wl:ParseHamiltonianExpression()` (lines 1430-1477) — Hamiltonian term parser
- `docs/derivation_performance.md` — profiling data and optimization notes

---

## Compute Hamiltonian after constraint elimination in Wolfram (Issue 7)

**Status:** DONE (Phase 1: Hamiltonian migration complete, Phase 2: EOM migration complete → Issue 8)

**Phase 1 (complete):** Constraint elimination runs in Wolfram BEFORE the Legendre transform: constraints detected in `fieldEquations`, substituted into `lagComp` via exact symbolic algebra, Hamiltonian computed for surviving fields only. Two constraint types:

1. **Gradient-zero**: `gradient(f)=0` → `f=0` (periodic BCs + zero IC)
2. **Degenerate algebraic**: self-field cancels (coeff≈1.0), residual constraint solved for dep field with largest |coefficient| (e.g., `h_9=h_4+h_7+h_9` → `0=h_4+h_7` → `h_4=-h_7`)

Nonlinearity warning (cannot occur in linearized regime but aids debugging). Dep field overwrite guard (`KeyExistsQ`) and field-product detection added for robustness.

**Phase 1 cleanup (complete, commit `694b134`):** Legacy `_substitute_hamiltonian_constraints()` fully removed (155 lines), `elim_substitutions` tracking removed, `wolfram_constraint_elimination` flag removed from `CanonicalStructure`.

**Phase 2 — Issue 8 (complete):** Full EOM migration. Split `_wls_canonical_hamiltonian()` → `_wls_canonical_phase_a()` (before JSON) + `_wls_canonical_phase_b()` (after JSON). Renamed `_wls_canonical_pipeline()` → `_wls_canonical_injection()`. All Python constraint elimination functions removed (~400 lines from `reduction.py`). JSON now born correct with surviving fields only.

**Verified:** scalar_field (|dE/E|=4.44e-09), coupled_scalars (|dE/E|=7.07e-09), gravitational_waves_1d 6→2 fields (|dE/E|=1.08e-08). All tests pass.

**Relevant code:**
- `tidal/cli/_derive.py:_wls_constraint_elimination()` — Wolfram constraint detection + substitution (~220 lines)
- `tidal/cli/_derive.py:_wls_canonical_phase_a()` — Lagrangian decomposition + constraint elimination (before JSON build)
- `tidal/cli/_derive.py:_wls_canonical_phase_b()` — IBP + Legendre transform + Hamiltonian (after JSON build)
- `tidal/cli/_derive.py:_wls_canonical_injection()` — volume element + canonical section injection into JSON
- `tidal/symbolic/reduction.py` — Python EOM-level elimination fully REMOVED (all 8 functions deleted)


## Full EOM Constraint Elimination Migration to Wolfram (Issue 8)

**Status:** DONE

**Problem:** EOM-level constraint elimination ran in Python post-processing (`reduction.py`) using fragile string-algebra helpers. `BuildMultiFieldJSONStructure` ran before constraint elimination, so JSON was born with all fields, then Python re-detected and re-eliminated.

**Solution implemented:**

Split into two phases and reordered in `_wls_metadata_and_export()`:

```
_wls_canonical_phase_a(ctx)                          ← decompose + constrain
BuildMultiFieldJSONStructure[fieldEquations, metadata] ← JSON has surviving fields only
_wls_canonical_phase_b(ctx)                          ← IBP + Legendre + H → inject into JSON
_wls_canonical_injection(ctx)                        ← volume element + canonical section
```

**Python functions fully removed** (~400 lines from `reduction.py`):
- `eliminate_degenerate_constraints()`, `_find_degenerate_constraint()`, `_find_gradient_zero_constraint()`
- `_substitute_in_terms()`, `_negate_symbolic()`, `_scale_symbolic()`, `_sum_symbolic()`
- Constants: `_DEGENERATE_TOL`, `_ZERO_TOL`, `_DERIVATIVE_ZERO_OPS`
- `_filter_coupling_matrices()` retained (used by `reduce_spec()`)

**Bug fix during migration:** Nonlinearity check in `_wls_constraint_elimination()` had false positives — counted all field symbols in the full expression rather than checking each additive term for multiplicative field products. Fixed to per-term check.

**Verified:** scalar_field (|dE/E|=4.44e-09), coupled_scalars (|dE/E|=7.07e-09), gravitational_waves_1d 6→2 fields (|dE/E|=1.08e-08). All tests pass.


## Part::partw Warning in Plane-Wave Reduction (Issue 9)

**Status:** FIXED (commit TBD)

**Symptom:** During derivation of plane-wave reduced theories (e.g., `gravitational_waves_1d`), Wolfram emits non-fatal warnings:
```
Part::partw: Part 3 of {2, 0} does not exist.
```

**Root cause:** Equation-level plane-wave reduction (`_wls_plane_wave_reduction_equations`, `_derive.py:1936`) uses:
```mathematica
Derivative[ords__][f_][args___] /; {ords}[[slot]] > 0 :> 0
```
When `slot=3` (z-coordinate in 3+1D) but `{ords}` has only 2 elements (short-arity derivative from Mathematica's internal simplification), `{ords}[[3]]` throws `Part::partw`.

The canonical pipeline avoids this because it normalizes derivative arities via `PadRight` before applying plane-wave rules. The equation-level reduction does not normalize.

**Fix:** Add length guard: `Length[{ords}] >= slot && {ords}[[slot]] > 0`. Applied at both locations:
- `_wls_plane_wave_reduction_equations()` — primary fix
- `_wls_canonical_phase_b()` — defense-in-depth

**Relevant code:**
- `tidal/cli/_derive.py:_wls_plane_wave_reduction_equations()`
- `tidal/cli/_derive.py:_wls_canonical_phase_b()`

---

### Issue: Wolfram JSON reduction — bare-coord rule corrupts `propagation_axis` metadata

**Status:** OPEN (2026-03-13)

**Symptom:** In the derived JSON, `"propagation_axis"` shows `"x"` instead of the original axis (e.g. `"z"`). The `Set::write: Tag List ... is Protected` Wolfram warning is also emitted.

**Root cause:** `_wls_json_plane_wave_reduction()` in `tidal/cli/_derive.py` sets reduction provenance metadata (including `"propagation_axis": "z"`) BEFORE running the string replacement rules. The bare-coordinate rule `"z" → "x"` (needed for `coordinate_dependent` arrays like `["z"] → ["x"]`) also hits the metadata value.

Additionally, after `ImportString[jsonStr, "JSON"]`, Mathematica returns nested `Rule` lists (not `Association`s), so the attempted post-hoc fix `jsonStructure["metadata", "reduction", "propagation_axis"] = "z"` fails with `Set::write: Tag List is Protected`.

**Fix:** Set reduction metadata AFTER the string replacement. Two approaches:
1. Inject the reduction metadata into the JSON string (via `StringReplace` or `StringInsert`) after all coordinate-remapping rules have been applied but before `ImportString`.
2. Don't re-import at all — keep the JSON as a string after replacement and write directly to file, bypassing the `ImportString` → `ExportString` round-trip.

**Relevant code:**
- `tidal/cli/_derive.py:_wls_json_plane_wave_reduction()`

---

### Issue: Wolfram `ImportString` returns Rules not Associations — Part assignment fails

**Status:** OPEN (2026-03-13)

**Symptom:** After `jsonStructure = ImportString[jsonStr, "JSON"]`, any subsequent Part assignment like `jsonStructure["metadata", "reduction", "propagation_axis"] = "z"` fails with:
```
Set::write: Tag List in {...} is Protected.
```

**Root cause:** Mathematica's `ImportString[..., "JSON"]` returns a nested structure of `Rule` lists, not `Association`s. The `jsonStructure["key", "subkey"] = value` syntax only works on `Association`s, not `Rule` lists.

**Impact:** Cannot modify the JSON structure after the `ExportString → StringReplace → ImportString` round-trip. This affects the reduction provenance metadata restore and any future post-replacement modifications.

**Fix options:**
1. Use `Association @@ ImportString[...]` or recursive conversion to Associations.
2. Use `ReplaceAll` on the Rule list: `jsonStructure /. ("propagation_axis" -> _) -> ("propagation_axis" -> "z")`.
3. Don't re-import — keep the JSON as a string and write it directly to file. The final `Export[outputPath, jsonStructure, "JSON"]` would become `WriteString[outputPath, jsonStr]`.

**Relevant code:**
- `tidal/cli/_derive.py:_wls_json_plane_wave_reduction()`
- Affects any Wolfram code that modifies `jsonStructure` after `ImportString`

---

### Issue: Energy measurement context caching — 30-50% timeseries speedup

**Status:** OPEN (2026-03-13)

**Problem:** `_prepare_hamiltonian_context()` (`tidal/measurement/_energy.py:900`) rebuilds coordinate arrays (`np.meshgrid`), GridInfo, and position-dependent coefficient evaluations **every snapshot** in a timeseries measurement. For a 10-snapshot measurement with Gertsenshtein (position-dependent dipolar B-field), this is 9× redundant work since the grid geometry and symbolic coefficients don't change between snapshots.

**Impact:** 30-50% speedup on timeseries energy/conversion/conservation measurements. Particularly impactful for parameter sweeps where many measurement calls are made.

**Fix:** Add `@cached_property` on `SimulationData`:
```python
@cached_property
def _hamiltonian_context(self) -> _HamiltonianContext | None:
    if self.spec.canonical is None:
        return None
    return _prepare_hamiltonian_context(self)
```
Then pass `ctx=data._hamiltonian_context` in `_compute_hamiltonian_from_canonical()` and `_compute_hamiltonian_per_field()` instead of `ctx=None`.

**Relevant code:**
- `tidal/measurement/_energy.py:_prepare_hamiltonian_context()` (line ~900)
- `tidal/measurement/_energy.py:_compute_hamiltonian_from_canonical()` (line ~972)
- `tidal/measurement/_energy.py:_compute_hamiltonian_per_field()` (line ~1106)

---

### Issue: Sweep spec caching — avoid 50× JSON re-parse on resume

**Status:** OPEN (2026-03-13)

**Problem:** When resuming a sweep with `--resume`, `_measure_existing()` calls `_measure_run()` for each already-completed point. Inside `_measure_run()` (`tidal/cli/_sweep.py:552`), the equation spec is re-loaded from JSON via `load_equation_system(spec_path)` for every single point. For a 50-point resumed sweep, this means parsing the same JSON file **50 times**.

**Impact:** Saves 5-10 minutes per resumed sweep (JSON parsing + validation overhead × 50 points).

**Fix:** Load the spec once at the sweep session level (before the loop in `_execute_sweep_sequential`, line ~883) and pass it through:
```python
from tidal.symbolic import load_equation_system
spec = load_equation_system(spec_path)  # Load ONCE
# Pass spec to all _measure_existing() and _run_single() calls
```
Modify `_measure_existing()` and `_run_single()` signatures to accept an optional pre-loaded `spec` parameter.

**Relevant code:**
- `tidal/cli/_sweep.py:_execute_sweep_sequential()` (line ~883)
- `tidal/cli/_sweep.py:_measure_run()` (line ~552)
- `tidal/cli/_sweep.py:_measure_existing()` (line ~902)

---

### Issue: Hamiltonian coefficient pre-resolution for position-dependent theories

**Status:** OPEN (2026-03-13)

**Problem:** In `_compute_hamiltonian_from_canonical()` (`_energy.py:1024`), the loop calls `_evaluate_single_hamiltonian_term()` once per Hamiltonian term per snapshot. Each call re-evaluates position-dependent coefficients from `term.coefficient_symbolic`. For Gertsenshtein with 18 Hamiltonian terms × 100 snapshots, this is 1800 coefficient evaluations when only 18 unique evaluations are needed (coefficients are time-independent).

**Impact:** 10-15% speedup on energy measurements for position-dependent theories.

**Fix:** Pre-resolve all Hamiltonian coefficients in `_prepare_hamiltonian_context()` (which runs once) and store as `ctx.term_coeffs`. Then `_evaluate_single_hamiltonian_term()` uses the pre-computed coefficient directly instead of re-evaluating. The `term_coeffs` list already exists in `_HamiltonianContext` but is only populated for some terms — extend to cover all terms uniformly.

**Relevant code:**
- `tidal/measurement/_energy.py:_prepare_hamiltonian_context()` (lines ~926-951)
- `tidal/measurement/_energy.py:_evaluate_single_hamiltonian_term()` (lines ~1086-1103)

---

### Issue: BC fast-path in operators — skip string parsing for pre-resolved BCs

**Status:** OPEN (2026-03-13)

**Problem:** `RHSEvaluator` pre-resolves boundary conditions to `AxisBCSpec` objects (`rhs.py:78-80`), but the operator functions (`gradient()`, `directional_laplacian()`, `laplacian()`, `cross_derivative()` in `operators.py`) re-check with `isinstance()` and potentially re-parse strings every call. For a 100-term system with 10K timesteps, this is ~200K redundant string-to-BC conversions.

**Impact:** 3-5% solver speedup.

**Fix:** Add a fast-path parameter `_bc_already_resolved: tuple[AxisBCSpec, ...] | None = None` to the 4 affected operator functions. When set, skip `_normalize_bc()` and `_resolve_axis_bc()`. `RHSEvaluator` passes the pre-resolved BCs directly.

**Relevant code:**
- `tidal/solver/rhs.py:_resolved_bcs` (lines ~78-80)
- `tidal/solver/operators.py:gradient()` (line ~701), `directional_laplacian()` (~906), `laplacian()` (~919), `cross_derivative()` (~934)
- Each function calls `_str_to_axis_bc()` or `_normalize_bc()` internally

---

### Issue: Derivation — move SeparateMetric inside per-term decomposition loop

**Status:** OPEN (2026-03-13)

**Problem:** In `_wls_canonical_phase_a()` (`_derive.py:2770`), `SeparateMetric` is applied to the entire Lagrangian before splitting into additive terms for decomposition. For Einstein-Maxwell theories with position-dependent backgrounds, this creates bloated intermediate expressions. Moving `SeparateMetric` inside the per-term loop (after `List @@ lagForCanon` splits the Lagrangian) would separate metrics on smaller, simpler expressions.

**Impact:** 5-15% peak memory reduction during canonical decomposition. Currently 922→170 MB for Gertsenshtein; could potentially reduce the 170 MB working set further. Also potential 1-2 min time savings from reduced expression size during Expand/Simplify.

**Risk:** Must verify that term-level `SeparateMetric` is equivalent to Lagrangian-level (it should be since `SeparateMetric` is linear/distributive over addition, but xAct may have edge cases with cross-term metric contractions).

**Relevant code:**
- `tidal/cli/_derive.py:_wls_canonical_phase_a()` (lines ~2747-2789)
- Wolfram: `SeparateMetric[CD]` applied to `lagForCanon` before `List @@ lagForCanon`

---

### Issue: Modal solver — NaN overflow with gradient coupling terms

**Status:** FIXED (2026-03-14)

**Problem:** The modal solver (`tidal/solver/modal.py`) produced NaN overflow for multi-field systems (e.g. Gertsenshtein) where some fields have zero initial conditions and the per-mode evolution matrix has degenerate eigenvalues across independent field blocks.

**Observed:**
1. **Uniform Gertsenshtein (constant coefficients, N=512):** NaN at t≈37.5. Zero-IC fields (h_7, a_0, a_2) grew exponentially.
2. **Centered dipolar Gertsenshtein (position-dependent, N=1024):** NaN at t≈16.5.

**Root cause (CORRECTED from initial analysis):**

Two interacting problems — not purely numerical roundoff as originally diagnosed:

1. **Genuine positive real eigenvalues (physics).** The operator block L(k) has eigenvalues μ = -k² - ¼B₀²κ² ± ½B₀κ√(4k²+¼B₀²κ²). When k < B₀κ/√2 (≈0.212 for B₀=0.3, κ=1), one μ > 0, giving real eigenvalues ±√μ in the evolution matrix — exponential growth/decay. This is the *physical* Gertsenshtein parametric conversion, not a bug. Total Hamiltonian is conserved because growth in one field is compensated by the coupling term. Note: the eigenvalue analysis at k≈2 (reported as ±2.161i, ±1.861i) was correct for THAT k value — all modes at k > 0.212 are purely oscillatory. The instability only affects low-k modes.

2. **Degenerate eigenvalue mixing (numerics).** The 12×12 per-mode matrix is block-diagonal: h₅↔a₁ (4×4), h₇↔a₂ (4×4, identical eigenvalues), a₀ (2×2), a₃ (2×2). `np.linalg.eig` on the full 12×12 matrix mixes eigenvectors across blocks with degenerate eigenvalues, projecting nonzero-IC h₅/a₁ components onto zero-IC h₇/a₂ modes. The ~1e-15 roundoff coefficients then grow exponentially via the physical growth rate.

Additionally, the a₀ equation has a wrong-sign Laplacian (coeff=-1.0), giving eigenvalues λ=±k (always growing), which is the fastest path to NaN.

**Fix:** Block-aware eigendecomposition in `_evolve_per_mode`:
- `_find_independent_blocks()` detects decoupled blocks via union-find on the matrix coupling graph
- Each block is eigendecomposed independently — no degenerate-eigenvalue mixing possible
- Blocks with all-zero IC are skipped entirely — output stays exactly zero
- Pre-multiplied V×y0_eigen for efficient inner loop (avoids redundant einsum per timestep)
- Eigenvalue growth warning when max(Re(λ))×Δt > 30
- `_evolve_full_matrix` also gets eigencoefficient cleanup (threshold at 1e-14 × max|y0_eigen|)

**Impact:** Modal solver now works correctly for Gertsenshtein and any multi-field system with independent blocks. Zero-IC fields remain at machine zero regardless of simulation time.

---

### Issue: Periodic coefficient check uses relative threshold — should weight by absolute magnitude

**Status:** OPEN (2026-03-13)

**Problem:** `CoefficientEvaluator.check_periodic_coefficient_continuity()` in `coefficients.py` computes `rel_jump = |coeff(L) - coeff(0)| / max(|coeff|)` and warns at >1%, errors at >50%. For centered dipolar Gertsenshtein, this gives false-positive warnings (2-6% relative jump) because the B-field is periodic but Wolfram expands `B(x)²·x²` with bare `x²` factors.

**Why the warnings are benign:** The absolute coefficient values at the boundary are O(10⁻⁵ to 10⁻²) because B(boundary) ≈ 0.0017 ≪ B(center) = 0.1. The IBP energy leak ∝ absolute coefficient jump × wave amplitude², both negligible at boundaries. Simulation confirms: |dE/E| = 4.53e-7.

**Proposed fix:** Weight the check by the absolute coefficient magnitude relative to its peak. A coefficient with 6% relative jump but absolute boundary value of 0.02 (vs peak of 10) is physically benign. The warning threshold should be based on `|coeff(L) - coeff(0)| / (1 + |coeff_peak|)` or similar.

**Impact:** Low — current warnings are conservative but not harmful. Reducing false positives would improve user experience.

**Relevant code:**
- `tidal/solver/coefficients.py:check_periodic_coefficient_continuity()`

---

## Early-stage plane-wave reduction and TT gauge to skip redundant decomposition

**Status:** OPEN (2026-03-14)

**Problem:** The derivation pipeline performs significant redundant work for theories with plane-wave reduction and TT gauge constraints:

Current flow: Full 3+1D Lagrangian → VarD (all 10 h-components + 4 a-components) → DecomposeToComponents (10+4=14 fields, ~332 terms each) → TT gauge + constraint elimination (14→6 fields) → Canonical/Hamiltonian (34 terms) → Plane-wave reduction (6→6 fields, 4D→2D coords)

The most expensive steps are BatchedTBD (decomposition) and the IBP Hamiltonian terms. We decompose all 10 h-components, but TT gauge immediately eliminates 4 (h_4, h_6, h_8, h_9), and plane-wave reduction makes 4 more trivial (a_0, a_3 decouple). The IBP then processes 34 Hamiltonian terms for all 6 fields when only 2 coupled pairs matter.

**Proposed optimization:** Apply TT gauge constraints (h_0μ=0, tr(h)=0) and plane-wave reduction (∂_x=∂_y=0) at the **Lagrangian level** before VarD. This would:
1. Zero the 4 eliminated h-components before decomposition → skip 4/10 of the h decomposition
2. Zero transverse derivatives before decomposition → simpler BatchedTBD traces
3. Reduce from 34 to ~8-10 Hamiltonian terms

**Complication:** xAct's `VarD` operates on abstract tensor indices and can't be told "only vary w.r.t. h_5 and a_1". It varies w.r.t. the full tensors `h_{ab}` and `a_μ`, then `DecomposeToComponents` extracts individual components. Moving reduction earlier would require substituting `h_{ab} → h_5·(e_5)_{ab} + h_7·(e_7)_{ab}` **before** `VarD` — effectively doing decomposition before variation, which changes the mathematical order.

Note that `_wls_plane_wave_reduction_lagrangian()` already exists and zeros transverse derivatives in the Lagrangian. This partially addresses point (2) but not points (1) or (3). Combining all three optimizations could potentially reduce derivation time from ~25 min to ~10 min for Gertsenshtein-class theories.

**Impact:** Medium — derivation takes ~25 min currently. Optimization would save ~15 min per derivation, significant for iterative development but not blocking.

**Relevant code:**
- `tidal/cli/_derive.py:_wls_plane_wave_reduction_lagrangian()` — existing partial implementation
- `tidal/cli/_derive.py:_wls_constraint_elimination()` — TT gauge constraint elimination
- `tidal/wolfram/ComponentDecompose.wl:DecomposeToComponents` — BatchedTBD bottleneck
- `tidal/cli/_derive.py:_wls_canonical_phase_b()` — IBP Hamiltonian computation

---

## Radial Gertsenshtein canonical decomposition: ~5-7 hour runtime

**Status:** OPEN (2026-03-16). Requires more compute resources — not blocked by code issues.

**Problem:** The canonical Lagrangian decomposition (Hamiltonian computation) for the radial Gertsenshtein theory (`examples/gertsenshtein/theory_radial.toml`) takes ~5-7 hours. The EOM decomposition completes in ~37 min. This is the only remaining step blocking validation of graviton-photon conversion in spherical coordinates with dipolar B-field.

**Root cause — TraceBasisDummy O(4^{2K}) enumeration:**

`TraceBasisDummy` in xAct/xCoba enumerates all index combinations for dummy basis indices. For a scalar expression with K contracted index pairs in 4D: 4^{2K} combinations. The Einstein-Hilbert sector (term 2/34) has complex contractions producing 82 tensor terms after `ToBasis`, each taking ~2 min for TraceBasisDummy → ~175 min for term 2 alone.

This is intrinsic to xAct's algorithm and cannot be reduced without fixing indices before TraceBasisDummy (only possible for the EOM path via `ExtractTensorComponent`, not for the scalar Lagrangian path).

**Critical finding — PD indices are abstract after ToBasis:**

After `ToBasis[chart]`, PD operators retain abstract xAct dummy indices (e.g. `-h$34637`), NOT concrete basis integers like `{2, -chart}`. Concrete indices only appear INSIDE TraceBasisDummy's internal enumeration. This means no pre-TraceBasisDummy zeroing of transverse PD operators is possible for the scalar Lagrangian path. Verified empirically via diagnostic print of PD patterns after ToBasis.

**Why metric DownValues work but PD DownValues don't:**

`SetMetricDownValues` (5.2x memory improvement) works because metric tensors are **multiplicative leaf factors** — zeroing `metric[{0,-chart},{1,-chart}] = 0` zeros the entire product before inner factors are evaluated. PD **wraps** inner expressions that are already evaluated, so zeroing PD adds overhead without saving the inner evaluation cost.

**Optimizations attempted (all benchmarked on fast examples first):**

| Approach | Commit | Result | Root Cause |
|---|---|---|---|
| Batched pipeline (OOM fix) | `0da51fe` | **Working** — 953 MB vs 10+ GB | Bounds peak memory per batch |
| Per-term plane-wave reduction | `139482b` | **Working** — 7/34 terms ZERO'd | Zeros Derivative patterns after ConvertCDToDerivatives |
| ComponentValue PD zeroing | `cd960c2` → `ea15759` | **+9-56% slower** (REVERTED) | xAct internal tables don't auto-evaluate; adds pattern overhead |
| Direct PD DownValues (specific ints) | tested, not committed | **+66% slower** | O(1) hash lookup per TraceBasisDummy eval; PD wraps inner exprs |
| ReplaceAll on PD after ToBasis | tested, impossible | **N/A** | PD indices are abstract after ToBasis |
| Together pre-simplification | `b2883fd` → `0632590` | **+30% slower** (REVERTED) | Restructures coefficients, slows downstream ToBasis+TraceBasisDummy |

**Possible future approaches (not yet implemented):**

1. **More compute resources**: Single-core CPU-bound (~100% utilization), memory-bounded at ~1 GB. A faster CPU or cloud instance helps linearly.
2. **Component-level Lagrangian decomposition**: Refactor `DecomposeScalarExpression` to fix some indices before TraceBasisDummy (like `ExtractTensorComponent` does for EOM). For rank-2 fields: 4^8 → 10 × 4^6 (~2.5x speedup). Significant refactoring required.
3. **Exploit term structure sharing**: Group Lagrangian terms sharing tensor skeleton, decompose representative once, scale by coefficient. Requires hash function on xAct tensor structure.

**Current state:**
- EOM decomposition: works (37 min, 1.2 GB). 10 surviving fields: h_4-h_9, a_0-a_3.
- Canonical decomposition: runs but takes ~5-7 hours. Memory stable at ~1 GB.
- Flat Gertsenshtein (all variants): fully validated.
- Validation target: P = sin²(κ/2 × Bpeak × z0³ / (2 × RNS²)) via Boccaletti formula.

**Relevant code:**
- `tidal/wolfram/ComponentDecompose.wl:DecomposeScalarExpression` — canonical decomposition pipeline
- `tidal/wolfram/ComponentDecompose.wl:BatchedTraceBasisDummyWithMetric` — batched TraceBasisDummy
- `tidal/cli/_derive.py:_wls_canonical_phase_a()` — canonical loop with per-term reduction
- `examples/gertsenshtein/theory_radial.toml` — theory config
