"""Figure 3 — Trace-channel ghost growth (§4.5, conditional).

Shows log|amplitude| vs t for a longer integration of the trace-channel
ghost-unstable parameter point, demonstrating exponential growth that
diagnoses the ghost.

This figure is conditional: the §4.5 drafting session decides whether to
include it based on data quality. If the time-domain signal is clean and
the diagnosis is unambiguous, include the figure; otherwise drop it and
report the diagnosis in prose.

Data source:  hpc_results/.../trace_ghost_diagnosis/  (TBD path)
Output:       manuscript/figures/fig3_trace_ghost_growth.pdf

Stub at Phase 2 scaffold time. Implementation lives in the per-figure
drafting session.
"""

from __future__ import annotations


def main() -> None:
    msg = (
        "fig3_trace_ghost_growth: implement during the §4.5 drafting session "
        "(conditional on data quality)."
    )
    raise NotImplementedError(msg)


if __name__ == "__main__":
    main()
