"""Figure D.3 — Channel comparison (App D).

Shows the time evolution of the conversion probability in the
hx<->ax channel side-by-side with the trace channel for the propagating-PGT
parameter point. Demonstrates that the two channels behave differently and
validates the channel-decomposition methodology.

Data source:  same as fig 2 — hpc_results/.../d22_shapiro_sup_intr/
Output:       manuscript/figures/figD3_channel_comparison.pdf

Stub at Phase 2 scaffold time. Implementation lives in the per-figure
drafting session.
"""

from __future__ import annotations


def main() -> None:
    msg = "figD3_channel_comparison: implement during the App D drafting session."
    raise NotImplementedError(
        msg
    )


if __name__ == "__main__":
    main()
