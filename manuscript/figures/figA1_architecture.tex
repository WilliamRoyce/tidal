\documentclass[border=10pt]{standalone}
\usepackage{tidal-tikz-styles}

\begin{document}
\begin{tikzpicture}[
    node distance=0.4cm
]

% ===================================================================
% ROW 1: Main pipeline stages (left to right)
% ===================================================================

% --- Input ---
\node[data] (toml) {Lagrangian};

% --- Derive ---
\node[stage, draw=IBMblue, fill=IBMblue!40, right=0.6cm of toml] (derive)
    {Symbolic derivation};

% --- JSON (narrow waist) ---
\node[data, right=0.6cm of derive, minimum width=2.6cm,
      font=\footnotesize\bfseries] (json)
    {Equation specification};

% --- Simulate ---
\node[stage, draw=IBMindigo, fill=IBMindigo!40, right=0.6cm of json] (sim)
    {Numerical integration};

% --- Snapshots ---
\node[data, right=0.6cm of sim] (snaps)
    {Field\\[-1pt]\scriptsize snapshots};

% --- Measure ---
\node[stage, draw=IBMindigo, fill=IBMindigo!40, right=0.6cm of snaps] (meas)
    {Post-processing};

% --- Results ---
\node[data, right=0.6cm of meas] (results)
    {Observables};

% ===================================================================
% ARROWS (main flow)
% ===================================================================
\draw[arr] (toml) -- (derive);
\draw[arr] (derive) -- (json);
\draw[arr] (json) -- (sim);
\draw[arr] (sim) -- (snaps);
\draw[arr] (snaps) -- (meas);
\draw[arr] (meas) -- (results);


% ===================================================================
% BRACE: Symbolic vs Numerical separation at JSON
% ===================================================================
\draw[decorate, decoration={brace, amplitude=6pt, raise=2pt},
      IBMblue, thick]
    (toml.west |- derive.north) -- (derive.north east)
    node[midway, above=10pt, font=\scriptsize\bfseries, text=IBMblue]
    {Symbolic (CAS)};

\draw[decorate, decoration={brace, amplitude=6pt, raise=2pt},
      IBMindigo, thick]
    (sim.north west) -- (meas.north east)
    node[midway, above=10pt, font=\scriptsize\bfseries, text=IBMindigo]
    {Numerical (PDE)};

\end{tikzpicture}
\end{document}
