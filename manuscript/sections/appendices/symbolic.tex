% Appendix B: Symbolic computation of the equations of motion (~1600 w).
% Per genre_conventions_software.md §(i). Renamed from "Symbolic stage"
% — the BHL convention is content-bearing names (compare PSALTer v2's
% "Construction of operators", "Moore--Penrose pseudoinversion") rather
% than pipeline-stage labels.

\section{Symbolic computation of the equations of motion}%
\label{SymbolicComputation}

\paragraph*{xAct environment}
% TODO: which packages are loaded; why \xPert is used for metric
% perturbations (not xTens alone); why xTras is needed for the
% Einstein–Cartan coupling.

\paragraph*{Lagrangian input from TOML}
% TODO: how a TOML config becomes a Wolfram session; representative
% user-session listing showing how to invoke `tidal derive`.

\paragraph*{Euler--Lagrange derivation}
% TODO: E-L velocity form (state, v, A0); not Legendre transform.

\paragraph*{Component decomposition}
% TODO: DecomposeToComponents, cross-field handling via additionalFields.
% Brief note on why component-level E-L is required.

% Placeholder for the gauge-fixing methodology. Source content in
% docs/tex/gauge_fixing.tex (the named presets — lorenz, de_donder,
% temporal, coulomb, axial, tt — and the custom Lagrangian-term
% mechanism for arbitrary user-specified gauges). Topics to cover when
% authored: the two mechanisms (gauge-fixing term added to the
% Lagrangian before E-L vs. constraint imposed after E-L); the named
% presets; how custom gauges are declared; how the choice interacts
% with the constraint elimination in the JSON export.
% Cross-referenced from \cref{Architecture}.
\paragraph*{Gauge fixing}%
\label{GaugeFixing}
% TODO: synthesise from docs/tex/gauge_fixing.tex.

\paragraph*{JSON export}
% TODO: which fields are written; how the canonical block is constructed.

\paragraph*{Failure modes and diagnostics}
% TODO: derivation timeouts, index inconsistency, malformed JSON; how
% each is diagnosed.

% Placeholder for the perturbative-reduction discussion. Source content in
% docs/tex/perturbative_reduction_constraint_barrier.tex (~550 lines).
% Topics to cover when authored: the constraint-promotion barrier (a
% small parameter promoting an algebraic constraint to a dynamical
% field), Ostrogradsky / Parker–Simon / JLM failure modes, structural
% analogues (vDVZ discontinuity, Boulware–Deser non-decoupling,
% Maxwell–Proca reclassification), and open research directions.
% Cross-referenced from \cref{Architecture} for the higher-derivative
% terms in the quadratic PGT action.
\paragraph*{Perturbative reduction and constraint promotion}%
\label{PerturbativeReduction}
% TODO: synthesise from docs/tex/perturbative_reduction_constraint_barrier.tex.
