% App G: Perturbative-regime bound in the torsionful background
% Forward-reference target from §2.2 (background ansatz) and §2.3
% (conversion-probability prose). Extends the Hwang & Noh
% Einstein--Maxwell perturbativity argument to PGT+EM theories.
% Project source: docs/tex/background_validity.tex (already documents
% the B_0 → 0 generalisation; this appendix presents the result in
% report form).

\section{Perturbative-regime bound in the torsionful background}
\label{PerturbativeRegime}

\paragraph*{Background-perturbativity in Einstein--Maxwell}
% TODO ~150 w. State the linearisation validity condition:
% |h_{μν}| ≪ 1 across the propagation distance, equivalently
% κ B_0 D ≪ 1 (so that P_conv = sin²(κ B_0 D / 2) ≪ 1).
% Cite hwangnoh2023graviton for the explicit second-order
% perturbation structure that motivates the bound. Note: the
% κ B_0 D ≪ 1 statement is our packaging of H-N's |h| ≪ 1 condition
% in a form directly comparable to campaign measurables.

\paragraph*{Extension to PGT torsion backgrounds}
% TODO ~200 w + eq. With propagating torsion, the effective coupling
% in the Gertsenshtein channel can pick up torsion-mass-scale-dependent
% factors. Reproduce the B_0 → 0 argument from
% docs/tex/background_validity.tex §sec:bv-b0-argument: the conversion
% coefficient C_0 = lim_{B_0 → 0} P / B_0² is finite, computed on a
% self-consistent flat / zero-torsion background, and free from
% backreaction effects. Backreaction enters at O(B_0) and vanishes in
% the limit. Verify against campaign results: across the swept
% parameter ranges in §4.2, P_max < 0.1 was confirmed numerically.
\begin{align}
  % TODO: C_0 definition and the |h| ≪ 1 bound in PGT generalisation
  \label{PerturbativeBound}
\end{align}

\paragraph*{Self-consistency of the §4 survey}
% TODO ~50 w. One sentence: across the parameter regions surveyed in
% §4 the perturbative bound is satisfied numerically; cross-ref
% \cref{SectorSurvey} for the per-theory P_max values.

\paragraph*{The $\Bzero \to 0$ survey coefficient}
\label{SurveyCoefficient}
% TODO ~150 w. State the C_0 = lim_{B_0 → 0} P / B_0^2 framework
% that our HPC campaigns measure: this is the well-defined survey
% observable, independent of B_0 in the small-B_0 limit, that
% characterises the conversion physics of each Lagrangian without
% B_0-dependent artefacts (per docs/tex/background_validity.tex
% §sec:bv-b0-argument). The framework relies on a gauge-consistent
% equation-of-motion structure for the linearised graviton-photon
% coupling, which agrees with the potential-formulation derivation
% in~\cite{hwangnoh2023graviton}. Backreaction enters at O(B_0)
% and vanishes in the limit, making C_0 the natural cross-theory
% comparison observable.
