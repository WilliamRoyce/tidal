% Appendix J: Bayesian inference architecture (v3, tachyon-permissive).
% Per genre_conventions_software.md §(h) — methodology appendix style.
% Audience: a reader who wants to understand how TIDAL's nested-sampling
% layer differs from PSALTer / Hamilcar and from a conventional
% gate-based likelihood.
%
% Source-of-truth: docs/V3_ARCHITECTURE.md (canonical reference),
% docs/V3_PHASE_D_DESIGN.md (manuscript update sequence), and the
% post-2026-05-08 supervisor meeting that pivoted v2 → v3.
%
% DRAFT STATUS: this appendix is a scaffold with TODO stubs.  Phase 2
% drafting fills in the prose with placeholder numbers later replaced
% by Phase B chain results.  The architecture text itself is settled
% and does NOT depend on Phase B / Phase E outcomes.

\section{Bayesian inference architecture}%
\label{InferenceArchitecture}

% Paragraph 1 — Motivation (~120 w).
\paragraph*{Motivation}
% TODO: state the problem v3 solves.  v2's hard-gate likelihood
% returned $\log\mathcal{L} = -\infty$ for tachyonic samples
% (eigenvalue probe $\gamma_{\mathrm{eff}} > 0.15$), non-perturbative
% samples ($P_{\max} > 0.5$, the Hwang--Noh validity boundary), and
% any numerical failure (divergence, NaN, exception).  This gives
% PolyChord no gradient information — the sampler cannot distinguish
% "almost stable" from "wildly unstable".  Reference the 8 May 2026
% supervisor pivot directing three coupled changes: (i) the probe is
% a measurement, not a gate; (ii) soft penalties replace $-\infty$
% for numerical failures; (iii) compactified priors cover the full
% real-line coupling support.
% Source: docs/V3_ARCHITECTURE.md §"Why v3?", docs/meetings/2026-05-08_supervisor.md.

% Paragraph 2 — Soft-penalty floor (~150 w + equation).
\paragraph*{Soft-penalty floor}
% TODO: describe the soft floor mathematically.  The replacement
% $\log\mathcal{L} = -\infty \to \log\mathcal{L}_{\mathrm{floor}} + \mathcal{N}(0, \sigma_{\mathrm{explore}}^2)$
% with $\log\mathcal{L}_{\mathrm{floor}} = -100$ and
% $\sigma_{\mathrm{explore}} = 1\;\mathrm{nat}$ default.  Rationale:
% $\exp(-100) \approx 4 \times 10^{-44}$ is small enough to act as
% effective rejection in PolyChord's posterior weighting, but large
% enough that the sampler sees a finite gradient
% ($\exp(-50)/\exp(-100) \approx 10^{22}$ ratio between a typical
% genuine sample and a floor sample).  The Gaussian noise prevents
% the sampler from seeing a flat plateau in the failure region.
% Discuss the seven distinct \texttt{run\_status} tags (success,
% simulation\_diverged, simulation\_failed, metric\_nan,
% metric\_missing, logl\_minus\_inf, below\_noise\_floor, exception,
% tachyonic\_gated under \texttt{-{}-gated}) and that post-chain
% analysis filters by tag.
% Source: tidal/inference/_likelihood.py, V3_ARCHITECTURE.md soft-penalty table.

% Paragraph 3 — Compactified priors (~120 w + table).
\paragraph*{Compactified priors}
% TODO: describe the prior compactification scheme.
% \texttt{arctan\_uniform:-89:89} for sign-symmetric dimensionless
% couplings ($\alpha_i$, $\beta_i$, $\delta_i$, $\chi$, $\zeta$):
% angle sampled uniformly in $[-89^\circ, 89^\circ]$, physical
% sample $x = \tan(\theta\pi/180)$ ranges over $\pm\tan(89^\circ)
% \approx \pm 57$ with Cauchy-like density peaking at zero.
% \texttt{log\_uniform:1e-3:1e3} for positive-definite kinetic
% coefficients ($\alpha_3$, $\xi$, $m_A^2$): six decades of coverage
% with $1/x\log(\mathrm{hi}/\mathrm{lo})$ density.  Show in a small
% table or inline list.  Frame the supervisor's rationale: narrow
% uniforms make the chain "rediscover" the natural scale by
% concentrating its likelihood density, but the wide prior support
% means the chain can actually \emph{explore} the high-coupling
% regime that v2 had ruled out by fiat.
% Source: V3_ARCHITECTURE.md compactified-prior table; tidal/inference/_prior.py.

% Paragraph 4 — Cubed-sphere joint prior (~120 w).
\paragraph*{Cubed-sphere joint prior}
% TODO: describe the higher-dimensional alternative.  Following the
% supervisor's unpublished \texttt{psalter} package, a coupling
% vector $\boldsymbol{c} \in \mathbb{R}^N$ can be sampled as
% $\boldsymbol{c} = r\,\hat{\boldsymbol{\theta}}$, with magnitude
% $r$ from \texttt{log\_uniform} and direction
% $\hat{\boldsymbol{\theta}}$ uniform on the unit sphere $S^{N-1}$
% via a gnomonic cubed-sphere chart (one tile per face, per
% sub-division).  The chart wraps a cube around the origin and
% projects each face radially out to the sphere, giving a
% quasi-rectilinear angular grid with no polar crowding.  Reference
% the parallel-session implementation in
% \texttt{tidal/inference/\_sphere.py} and the corresponding atlas
% plot that pools per-tile chains into a 2N-face panel grid.
% Source: docs/V3_PHASE_C_REFERENCE.md, tidal/inference/_sphere.py.

% Paragraph 5 — Validation (~80 w).
\paragraph*{Architecture validation}
% TODO: D1 amp v3 smoke run (Phase B.1, HPC job 29149987) verified
% the architecture admits the predicted parameter regions and
% produces a 100\% \texttt{run\_status=success} chain.  Headline
% result: log $\mathcal{Z}$ rose from $+0.72$ (v2) to
% $+[\textrm{B.2 log Z}]$ (v3) for the D1 amplification chain, and
% the maximum-a-posteriori value of $\delta_1$ landed outside v2's
% narrow $[-2, +2]$ prior — a region only reachable under the
% compactified \texttt{arctan\_uniform} scheme.  Per-coupling
% marginal $D_{\mathrm{KL}}$ replaces log $\mathcal{Z}$ as the
% headline metric: it measures which couplings carry
% signal-shape information.  See \cref{Results}.
% Source: hpc_results/29149987/, docs/comparison/d1_amp_v2_v3.md.

% Paragraph 6 — Caveat (~60 w).
\paragraph*{Linearised-theory validity}
% TODO: caveat — large $P_{\max}$ values $\gtrsim 1$ may fall outside
% the linearised-PDE regime in which TIDAL operates.  The v3
% architecture does not gate on this: $P_{\max}$ is recorded
% faithfully and the question of whether a high-$P$ region is
% physically meaningful is downstream of inference.  Forward-reference
% to App~G perturbative-regime analysis.
% Source: docs/V3_PHASE_E_DESIGN.md (geometry pivot for finite
% interaction time).
