% !TEX root = ../main.tex
% Results scaffold (Phase 3): four-subsection architecture compiled from the
% v3 post-unblock-filter HPC survey under the plane-wave uniform-Bzero
% benchmark.  The plane-wave benchmark is the first pass of a multi-pass
% survey; the localised finite-Bzero-region campaign is the load-bearing
% next step (the BHL "future work" framing for it lives in §5 Discussion,
% never in §4 body prose).
%
% Structure (target ~1900 w):
%   §4 opening (~100 w): BHL Pattern A+B — recall the action, anchor
%      Apps I, G, J; introduce both amplification quantities (\Aamp from
%      App G, A(θ) from App J).
%   §4.1 \label{ValidationResult} (~290 w): Validation against the
%      Einstein--Maxwell baseline; figure 1 the Boccaletti reproduction.
%   §4.2 \label{SurveySummary} (~300 w): Plane-wave-benchmark survey
%      summary with paired-Bayes-factor table.  Carries \label{SectorSurvey}
%      and \label{SectorSurveyTable} as compatibility forwards.
%   §4.3 \label{InformativeAmplification} (~700 w): sectors with
%      informative posteriors favouring amplification — T1 (Stage A pub),
%      T4 (D1 pub), T5.0–T5.3 progression (D2.0 / D2.1 pub partial, D2.2 /
%      D2.3 landscape).  Carries \label{ChannelStructure} as compatibility
%      forward for theory.tex line 377 cross-ref.
%   §4.4 \label{FrontierAndNulls} (~550 w): plane-wave-benchmark
%      frontier nulls — T3 analytic null (R²-PGT), T6-minimal suppression
%      preference, T6 full and T7 floor-bound geometric nulls.  Brief
%      Einstein–Cartan minimal (T2) baseline note + surveyed-scope
%      paragraph.
%
% Headline finding emerges from §4.3 (T1 ≈ 450:1 amplification at the
% strongest end; full T5.3 9D ≈ 1600:1).  Cross-sector synthesis defers
% to §5 Discussion.
%
% Sign convention (per App J \cref{NestedScore}): log L = ±log A(θ);
% + sign rewards amplifying regions, − sign rewards suppressing regions.
% Bayes factor log B = log Z_amp − log Z_sup;  positive ⇒ amplifying
% regions dominate the prior-weighted likelihood.  All quoted numbers
% come from V3_PHASE_TRACKER.md cross-checked against the chain
% inference.json files.

\section{Results}%
\label{Results}

% Opening: BHL Pattern A (recall the action) + B (state the object of
% study).  No TOC-style subsection enumeration.  Anchors the appendix
% infrastructure (App I, G, J) and introduces both amplification
% quantities (\Aamp from App G, A(θ) from App J) used throughout §4.
To survey the Gertsenshtein conversion across the Poincar\'{e} gauge
theory of \cref{QuadraticAction} we partition the general quadratic
action by sector and probe each restriction in turn. The operator
inventory is collected
in \cref{PGTEnumeration}; the perturbative-regime conditions
under which the linearised treatment remains self-consistent are set
out in \cref{PerturbativeRegime}; and the Bayesian inference methodology is described in
\cref{InferenceArchitecture}. Throughout, the per-sample conversion
score $A(\theta)$ of \cref{AmplificationScore} drives the Bayesian
likelihood; the small-$\Bzero$ asymptotic ratio $\Aamp$ of
\cref{AmplificationFactor} additionally applies to the plane-wave
uniform-$\Bzero$.

\subsection{Validation against the Einstein--Maxwell baseline}%
\label{ValidationResult}
% Three named paragraphs: setup, Boccaletti reproduction, survey
% observables.  The survey-observables paragraph carries the App G + App J
% amplification-metric cross-refs that anchor the per-theory verdicts in
% §4.2–§4.4.

\paragraph*{Validation setup}
We validate the pipeline on the Boccaletti conversion
formula (\cref{GertsenshteinFormula}) in Einstein--Maxwell theory.
A uniform background field $\Bzero$ fills a periodic domain of length
$L=100$; the linearised system is initialised as a plane wave on a
$N=512$ grid and evolved to $t_\text{end}=50$, with photon carrier
wavenumber $k\approx2.011$ ($2\pi n/L$ mode, $n=32$),
sampling $\Bzero$ at 24 uniformly spaced values in $[0.005,0.25]$.

\paragraph*{Reproduction of the Boccaletti kernel}
\TIDAL{} recovers the Boccaletti kernel \cref{GertsenshteinFormula}
in the small-$\Bzero$ limit: the signed residual $P_{\TIDAL} - P_\text{bare}$
lies at the modal-evolution round-off floor of $\lesssim 10^{-8}$
(\cref{BoccalettiValidation}); the full two-mode comparison against
the detuning formula of~\cite{raffelt1988mixing} is in
\cref{BoccalettiUniform}.
The path-integrated kernel \cref{BoccalettiPathIntegrated} is recovered in
\cref{BoccalettiLocalised}.


% Figure 1 — Boccaletti validation. Rendered from
% scripts/figures/fig1_boccaletti_validation.py using
% benchmark_results/canonical/boccaletti_uniform_rich.json.
\begin{figure}[t]
  \centering
  \includegraphics[width=\columnwidth]{figures/fig1_boccaletti_validation}
  \caption{Boccaletti validation for the Einstein--Maxwell baseline
    on a flat Minkowski background with uniform
    $\Bzero \in [0.005, 0.25]$, domain length $L=100$,
    propagation time $t_\text{end} = 50$, carrier wavenumber
    $k \approx 2.011$ ($n=32$, $2\pi n/L$), on a periodic $N = 512$ grid. Upper
    panel: simulated $P_\text{final}(\Bzero)$ (filled circles)
    overlaid with the analytic $\sin^2(\kappa\Bzero t_\text{end}/2)$
    kernel (dashed line). Lower
    panel: signed residual $P_{\textit{TIDAL}} - P_\text{bare}$ on the
    same axis. The detailed two-mode comparison is given in
    \cref{BoccalettiUniform}.}
  \label{BoccalettiValidation}
\end{figure}

% \subsection{Survey summary under the plane-wave approximation (\cref{Linearisation})}%
% \label{SurveySummary}%

% Discussion preserved as comment block (per planning directive 2026-05-25):
% The headline log B range expanded to +13 once T7 v2 (complete parity-even
% χ-closure, 18D) was rescued at t_end=1 from the t_end=10 modal-solver
% overflow ceiling (max_eigenvalue × t_end > 708). The parity-split insight
% is the load-bearing physics: parity-even sectors have bounded eigenvalues
% on the plane-wave background and rescue cleanly under t_end=1, while
% parity-odd sectors (T6, T8) generate unbounded eigenvalue growth at any
% viable horizon (Re λ ≈ 1.4e3 for T6 at t_end=1, ≈ 5.5e5 for T8) —
% structural geometric incompatibility rather than methodology failure.
% NP (ξ=0) is co-headlined despite being labelled a "control" because the
% ghost-mediated interpretation was the most damaging counter-argument to a
% positive Gertsenshtein prediction in the propagating-torsion theories;
% ruling it out promotes NP to load-bearing headline status.


% Survey results table.  Sourced from V3_PHASE_TRACKER.md and the
% inference.json files of each listed chain.  Where landscape and
% pub-quality runs both exist for the same sub-theory, the pub-quality
% numbers are reported and the landscape source noted in the footnote.
% \begin{table*}[t]
%   \centering
%   \caption{Plane-wave survey results across the
%     surveyed sectors of the PGT hierarchy. Columns: theory class, prior
%     dimension, log evidence of the amplification-rewarding model
%     $\log\mathcal{Z}_+$ (per \cref{NestedScore} with $+\log A$), log
%     evidence of the suppression-rewarding model $\log\mathcal{Z}_-$
%     (with $-\log A$), Bayes factor $\log B = \log\mathcal{Z}_+ -
%     \log\mathcal{Z}_-$~\cite{Skilling:2006gxv}, quality label, and the qualitative verdict.
%     Errors $\sigma_{\log\mathcal{Z}}$ from the PolyChord
%     nested-sampling estimator. Landscape runs use
%     $n_\text{live} = 25\,d$ in the coupling dimension; publication
%     runs use $n_\text{live} \geq 50\,d$ or the values quoted in
%     \cref{InferenceArchitecture}. Higher-dimensional parity-even
%     sectors (the complete $\chi$-closure, the $\xi$-kinetic closure)
%     and the parity-odd sectors use the overflow-safe horizon
%     $t_\text{end} = 1$ rather than the publication horizon
%     $t_\text{end} = 10$ used by the lower-dimensional sectors; the
%     per-sample conversion score $A(\theta)$ of
%     \cref{AmplificationScore} is $t_\text{end}$-independent within
%     the perturbative regime of \cref{PerturbativityHierarchy}, so
%     Bayes factors are mutually comparable across the two settings.}
%   \label{SurveyResultsTable}%
%   \label{SectorSurveyTable}%
%   \begin{tabular}{lcrrrll}
%     \hline\hline
%     Theory class & $d$ & $\log\mathcal{Z}_+ \pm \sigma$ & $\log\mathcal{Z}_- \pm \sigma$ & $\log B$ & quality & verdict \\
%     \hline
%     dark-photon plasma                  & 4  & $+9.034 \pm 0.068$  & $+2.924 \pm 0.083$  & $+6.110$ & publication                            & \emph{amplifies} \\
%     EM--RC curvature coupling ($\Del{1}$) & 4  & $+13.374 \pm 0.060$ & $+11.395 \pm 0.097$ & $+1.979$ & publication                            & \emph{amplifies} \\
%     Yang--Mills--PGT (Bahamonde base)   & 5  & $+7.680 \pm 0.150$  & $+5.837 \pm 0.143$  & $+1.843$ & publication                            & \emph{amplifies} \\
%     Yang--Mills--PGT (Barker extension) & 6  & $+5.319 \pm 0.169$  & $+2.524 \pm 0.158$  & $+2.795$ & publication\textsuperscript{a}         & \emph{amplifies} \\
%     Yang--Mills--PGT (Shapiro extension)& 8  & $+8.889 \pm 0.135$  & $+6.212 \pm 0.176$  & $+2.677$ & landscape                              & \emph{amplifies} \\
%     Yang--Mills--PGT (non-minimal union) & 9  & $+10.034 \pm 0.142$ & $+2.662 \pm 0.156$  & $+7.372$ & landscape                              & \emph{amplifies} \\
%     non-propagating torsion ($\xi=0$, single $\chi$ control) & 8 & $+10.98 \pm 0.15$ & $+2.67 \pm 0.10$ & $+8.31$  & publication                            & \emph{amplifies (control)} \\
%     complete parity-even ($\chi$-closure) & 18 & $+10.30 \pm 0.08$  & $-3.18 \pm 0.07$    & $+13.48$ & publication\textsuperscript{a}         & \emph{amplifies} \\
%     non-propagating torsion ($\xi=0$, $\chi$-closure control) & 17 & $+9.93 \pm 0.08$ & $-3.28 \pm 0.07$ & $+13.21$ & publication\textsuperscript{a}      & \emph{amplifies (control)} \\
%     $\xi$-kinetic closure               & 32 & $-0.18 \pm 0.07$    & $-9.61 \pm 0.09$    & $+9.43$  & publication\textsuperscript{a,b}       & \emph{amplifies (partial rescue)} \\
%     parity-odd minimal sub-sector       & 4  & $+5.002 \pm 0.280$  & $+11.331 \pm 0.311$ & $-6.329$ & landscape                              & \emph{suppresses} \\
%     parity-odd full                     & 20 & $-15.01 \pm 0.023$  & $-14.99 \pm 0.025$  & $\approx 0$ & publication\textsuperscript{a,c}    & \emph{plane-wave overflow} \\
%     complete parity-odd                 & 53 & --                  & --                  & --       & publication\textsuperscript{a,d}       & \emph{plane-wave overflow} \\
%     Euler--Heisenberg + Gertsenshtein   & 2  & $+0.184 \pm 0.001$  & $-0.185 \pm 0.0004$ & $+0.37$  & publication                            & \emph{null (QED-EFT inert)} \\
%     \hline\hline
%   \end{tabular}\\[1ex]
%   {\footnotesize \textsuperscript{a} Run at the overflow-safe horizon
%    $t_\text{end} = 1$; the lower-dimensional sectors above use
%    $t_\text{end} = 10$.\quad
%    \textsuperscript{b} Both $\log\mathcal{Z}$ negative indicates partial
%    floor contamination from the extended $\xi_{11\!-\!16}$ sub-sector;
%    the quoted $\log B$ is a lower bound on the genuine amplification.\quad
%    \textsuperscript{c} Maximum eigenvalue $\mathrm{Re}\,\lambda \approx
%    1.4 \times 10^{3}$ at $t_\text{end} = 1$ — still above the
%    $\exp(708)$ modal-solver overflow ceiling; both posteriors collapse
%    to the soft-floor noise band.\quad
%    \textsuperscript{d} 0 dead points: $\mathrm{Re}\,\lambda \approx
%    5.5 \times 10^{5}$, requiring $t_\text{end} \lesssim 10^{-3}$ at
%    which $P \propto (\kappa\Bzero t/2)^2$ falls many orders below the
%    soft-floor noise band.\par}
% \end{table*}

\subsection{Surveyed sectors}%
\label{SectorSurvey}%

% Under the plane-wave approximation (\cref{Linearisation}) the
% surveyed PGT hierarchy yields the Bayes-factor partition collected in
% \cref{SurveyResultsTable}.
The Bayes factor
$\log B = \log\mathcal{Z}_+ - \log\mathcal{Z}_-$~\cite{Skilling:2006gxv}
quantifies amplification preference ($\log B > 0$) or suppression
preference ($\log B < 0$) relative to the Einstein--Maxwell baseline.
Whether the surviving amplification preferences reflect genuine
conversion-channel enhancement or tachyonic growth that a finite-extent
geometry would saturate is discussed in \cref{Discussion}.

\subsubsection{The $\MAGFt{}^2$-PGT sector}
We open with the $\MAGFt{}^2$-PGT sector, the minimal-coupling GR extension
wherein torsion propagates as a dynamical field. The Lagrangian retains the three
parity-even torsion-squared invariants $I_{1\!-\!3}$ of
\cite{shapiro2002torsion,hayashi1979new} together with the
$\Alp{1}\MAGFt{}^{2}$ curvature-squared term,
\begin{align*}
  \lag_{R^2}
    = -\tfrac{1}{\kappa^2}\,\MAGFt{}
       + \sum_{i=1}^{3} \Bet{i} I_i
    - \tfrac{1}{4}\FieldF{_{\alpha\beta}}\FieldF{^{\alpha\beta}}
       + \Alp{1}\,\MAGFt{}^{2} \, ,
  % \label{R2PGTLagrangian}
\end{align*}
giving a four-dimensional prior over
$\{\Bet{1\!-\!3}, \Alp{1}\}$. The Gertsenshtein channel is unaffected
by the $\Alp{1}$ coupling at linear order: the $\Alp{1}$-weighted
terms are absent from the channel equations that mediate the standard
Gertsenshtein conversion,
\begin{align*}
  \partial_t^2 A_x &= B_0 \, \partial_z h_{xy} + \partial_z^2 A_x, \\
  -\tfrac{1}{\kappa^2} \partial_t^2 h_{xy} &= \tfrac{1}{2} B_0^2 \, h_{xy}
    + B_0 \, \partial_z A_x - \tfrac{1}{\kappa^2} \partial_z^2 h_{xy}.
\end{align*}
\Cref{PropagatingKineticOutput} shows the full symbolic output.
The $\Alp{1}\MAGFt{}^{2}$ term promotes the metric-perturbation
sector to a fourth-order system, placing this theory outside the
scope of the numerical pipeline
(\cref{PerturbativeReduction,SymbolicMechanism}).
% ; no published
% constraint-promotion Hamiltonian recipe exists for the
% $\Alp{1}\MAGFt{}^{2}$ case~\cite{blagojevic2018hamiltonian}.
Torsion-fields do appear coupled
to the trace sector of the metric perturbation, however, the fourth-order time
derivatives in that sector (\cref{PropagatingKineticOutput}) introduce Ostrogradsky
ghost modes~\cite{Woodard:2015zca,blagojevic2018hamiltonian,lin2019pgt}.
We conclude that the minimal-PGT family does not stably
couple torsion to the Gertsenshtein effect at linear order.

\begin{figure}[t]
  \centering
  \includegraphics[width=\columnwidth]{figures/kl_carrier_corner_ricci_em}
  \caption{Overlaid amplification (magenta) and suppression (yellow) corner
    plot for the EM--RC curvature coupling sector. The $\Del{1}$ marginal separates
    cleanly: amplification extends broadly while suppression concentrates sharply
    at $\Del{1} \approx 0$.}
  \label{CornerT4}
\end{figure}

\subsubsection{Electromagnetic coupling to the Riemann--Cartan curvature}
To break this decoupling we introduce the first
non-minimal vertex: the direct coupling
$\Del{1}\MAGFt{_{[\mu\nu]}}\FieldF{^{\mu\nu}}$ between the
antisymmetric Riemann--Cartan Ricci tensor and the electromagnetic
field strength, introduced by~\cite{bahamonde2025torsion} and not
previously investigated in this context.
This theory sources the linearised conversion amplitude
directly at $\mathcal{O}(\Bzero)$ \emph{without} propagating torsion.
The lagrangian is
\begin{align}
  \lag_\text{EM--RC}
    = -\tfrac{1}{\kappa^2}\,\MAGFt{}
      + \Bet{i} I_{i}
      - \tfrac{1}{4}\FieldF{_{\mu\nu}}\FieldF{^{\mu\nu}}
      + \Del{1}\,\MAGFt{_{[\mu\nu]}}\FieldF{^{\mu\nu}} \, ,
   \label{RicciEMLagrangian}
\end{align}
giving a four-dimensional prior over
$\{\Bet{1\!-\!3}, \Del{1}\}$. Nested sampling gives
\begin{equation*}
  \log\mathcal{Z}_+ = +13.37 \pm 0.06
  \qand
  \log\mathcal{Z}_- = +11.40 \pm 0.10,
\end{equation*}
thus both directions show informative evidence, and the vertex supports both
significant amplification and suppression at different parameter values.
The Bayes factor $\log B = +1.98$ records that the prior-weighted integral is
marginally amplification-dominated. The $\Del{1}$-direction
carries $D_\mathrm{KL}(P_+\|P_-) = 20.8$~nats of
distinction between the two directions, over an order of
magnitude above any other coupling; the
$\Del{1}\MAGFt{_{[\mu\nu]}}\FieldF{^{\mu\nu}}$ operator itself is what drives
the bidirectional behaviour.

\begin{table}[h]
  \centering
  \begin{tabular}{l | ccc}
  \toprule
  & $D_\mathrm{KL,+}$ & $D_\mathrm{KL,-}$ & $D_\mathrm{KL}(P_+\|P_-)$ \\
  \midrule
  $\delta_1$ & 1.43 & 3.00 & 20.78 \\
  $\Bet{1}$ & 3.18 & 2.87 & 0.71 \\
  $\Bet{2}$ & 2.94 & 2.77 & 0.54 \\
  $\Bet{3}$ & 0.00 & 0.15 & 0.20 \\
  \bottomrule
  \end{tabular}
  \caption{EM--RC curvature coupling ($\Del{1}$) sector \cref{RicciEMLagrangian}: per-coupling $D_\mathrm{KL}$
    carrier identification for the plane-wave survey.}
  \label{KLTableDelta}
\end{table}

\subsubsection{The parity-odd minimal sub-sector}
A four-dimensional parity-odd sub-sector
$\{\Bet{1\!-\!3}, d_{21}\}$, retaining the single
Chern--Simons-type Nieh--Yan coupling $d_{21}$ on top of the
Einstein--Cartan minimal base, was scanned at landscape quality. Nested sampling gives
\begin{equation*}
  \log\mathcal{Z}_+ = +5.00 \pm 0.28
  \qand
  \log\mathcal{Z}_- = +11.33 \pm 0.31,
\end{equation*}
($\log B = -6.33$). The overlaid posterior of \cref{CornerT6Min}
resolves the asymmetry along $d_{21}$, in sharp contrast with the
parity-even sectors surveyed below.  The
$d_{21}$ direction carries $D_\mathrm{KL}(P_+\|P_-) = 8.8$ nats of
amp/sup separation (\cref{KLTableParityOddMin}) --- the Nieh--Yan
invariant is the single dominant carrier of the suppression
preference.

\begin{figure}[t]
  \centering
  \includegraphics[width=\columnwidth]{figures/kl_carrier_corner_parity_odd_minimal}
  \caption{Carrier sub-corner for the four-dimensional parity-odd
    minimal sub-sector $\{\Bet{1\!-\!3}, d_{21}\}$ at
    the landscape chain. The top-$K$ parameters ranked by combined
    marginal-amp / marginal-sup / cross-amp-sup $D_\mathrm{KL}$ are
    plotted; the suppression posterior concentrates
    at small $\{\Bet{i}\}$ and small $|d_{21}|$ while the
    amplification posterior is broader and offset, giving the
    survey's only clear suppression preference among the parity-odd
    sectors examined here.}
  \label{CornerT6Min}
\end{figure}

\begin{table}[h]
  \centering
  \begin{tabular}{l | c c c}
  \toprule
  & $D_\mathrm{KL,+}$ & $D_\mathrm{KL,-}$ & $D_\mathrm{KL}(P_+\|P_-)$ \\
  \midrule
$d_{21}$ & 1.79 & 3.04 & 8.83 \\
$\beta_1$ & 3.00 & 3.10 & 5.44 \\
$\beta_2$ & 3.10 & 3.40 & 4.35 \\
$\beta_3$ & 2.53 & 2.57 & 0.81 \\
  \bottomrule
  \end{tabular}
  \caption{Parity-odd minimal sub-sector: per-coupling $D_\mathrm{KL}$
    carrier identification for the plane-wave-benchmark survey
    (\cref{CornerT6Min} shows the top-$K$ carrier parameters).}
  \label{KLTableParityOddMin}
\end{table}

\subsubsection{The Yang--Mills--PGT}
Many authors have introduced non-minimal operators into PGT with different physical
motivations, and the $\Del{1}$ vertex in \cref{RicciEMLagrangian} is just one of several that could break
the minimal decoupling. To probe what each family contributes to the Gertsenshtein
conversion we consider four nested sub-theories that each turn on one
further family, isolating their effect.

The progression opens with the \textit{Bahamonde base}~\cite{bahamonde2025torsion},
which extends the $\Del{1}$ vertex by the torsion kinetic operator
$\xi(\nabla\mathcal{T})^2$, making torsion a dynamical propagating field for the
first time in the hierarchy.
The \textit{Barker extension}~\cite{barker2024poincare} adds the derivative-mixing
operator $\chi\MAGFt{_{[\alpha\beta]}}\mathcal{F}^{(T)\,\alpha\beta}$, identified
as the completion that exposes the Yang--Mills-like kinetic structure
of vector torsion absent from earlier PGT formulations.
The \textit{Shapiro extension}~\cite{shapiro2002torsion} adds the three
$\zeta_{1,2,3}\,\rcD{_\alpha}\mathcal{T}{\cdot}\FieldF{}$ contractions.
The \textit{non-minimal union} retains all three families simultaneously.
The combined Lagrangian is
\begin{widetext}
\begin{align}
  \lag_\text{YM--PGT}
    = \lag_\text{EM--RC}
      - \tfrac{1}{4}\xi\,
              \mathcal{F}^{(T)}_{\alpha\beta}\,
              \mathcal{F}^{(T)\,\alpha\beta}
    + \chi\,\MAGFt{_{[\alpha\beta]}}\,
              \mathcal{F}^{(T)\,\alpha\beta}
    + \zeta_1\,\rcD{_\alpha}\mathcal{T}^{\beta}{}_{\beta\gamma}\,
              \FieldF{^{\alpha\gamma}}
    + \zeta_2\,\rcD{_\alpha}\mathcal{T}^{\alpha}{}_{\beta\gamma}\,
              \FieldF{^{\beta\gamma}}
    + \zeta_3\,\rcD{_\alpha}\mathcal{T}_{\beta}{}^{\alpha}{}_{\gamma}\,
              \FieldF{^{\beta\gamma}} \, ,
  \label{NonMinimalLagrangian}
\end{align}
\end{widetext}
enumerated against the full
$\rcD{}\mathcal{T}{\times}\FieldF{}$ class in
\cref{DerivativeExtensions}. Across the four sub-theories the amplification
preference strengthens monotonically as the coupling space expands: the
Bayes factors $\log B = +1.84$, $+2.80$, $+2.68$, $+7.37$ grow as each
new operator family adds a further amplification corner. Both directions
remain informatively populated throughout --- suppression log-evidences lie
between $+2.5$ and $+6.2$ --- indicating amplification dominance without
the suppression corner collapsing. The carrier directions migrate across the
progression (tables below): the Bahamonde and Barker sub-theories are
dominated by the $\xi(\nabla\mathcal{T})^2$ kinetic direction (${\approx}5$
nats), the Shapiro sub-theory by $\Bet{1}$, and the non-minimal union by the
Barker $\chi$ vertex ($D_\mathrm{KL}(P_+\|P_-) = 3.9$ nats). Only the single
Barker $\chi$ appears at nine-parameter level; the full $\chi_{1\!-\!10}$
closure is the subject of the complete parity-even sector below.

\begin{figure}[t]
  \centering
  \includegraphics[width=\columnwidth]{figures/kl_carrier_corner_ym_pgt_bahamonde}
  \caption{Carrier sub-corner for the Bahamonde base of the
    Yang--Mills--PGT progression at the publication-quality chain.
    The top-$K$ parameters ranked by combined marginal-amp /
    marginal-sup / cross-amp-sup $D_\mathrm{KL}$ are plotted.
    The amplification posterior concentrates at low $\xi$ with broad
    $\Del{1}$, while the suppression posterior covers a wider $\xi$
    band but is sharply localised at $\Del{1} \approx 0$.}
  \label{CornerT5D20}
\end{figure}

\begin{figure}[t]
  \centering
  \includegraphics[width=\columnwidth]{figures/kl_carrier_corner_ym_pgt_barker}
  \caption{Carrier sub-corner for the Barker extension of the
    Yang--Mills--PGT progression at the publication-quality chain.
    The top-$K$ parameters ranked by combined marginal-amp /
    marginal-sup / cross-amp-sup $D_\mathrm{KL}$ are plotted.
    The added $\chi$ direction broadens the amplification posterior
    into a non-axis-aligned region in the $(\Del{1}, \chi)$ plane.}
  \label{CornerT5D21}
\end{figure}

\begin{figure}[t]
  \centering
  \includegraphics[width=\columnwidth]{figures/kl_carrier_corner_ym_pgt_shapiro}
  \caption{Carrier sub-corner for the Shapiro extension of the
    Yang--Mills--PGT progression at the landscape chain.
    The top-$K$ parameters ranked by combined marginal-amp /
    marginal-sup / cross-amp-sup $D_\mathrm{KL}$ are plotted.
    The Shapiro derivative couplings $\zeta_{1,2,3}$ tighten
    the suppression sub-region near $\zeta_i \approx 0$ while
    leaving the amplification posterior structure inherited from
    the Bahamonde base largely unchanged.}
  \label{CornerT5D22}
\end{figure}

\begin{figure}[t]
  \centering
  \includegraphics[width=\columnwidth]{figures/kl_carrier_corner_ym_pgt_full_nonminimal}
  \caption{Carrier sub-corner for the non-minimal union of the
    Yang--Mills--PGT progression at the landscape chain. The top-$K$
    parameters ranked by combined marginal-amp / marginal-sup /
    cross-amp-sup $D_\mathrm{KL}$ are plotted. The accumulated
    amplification structure across all nine couplings yields the
    strongest Bayes factor of the parity-even progression under the
    plane-wave approximation ($\log B = +7.37$).}
  \label{CornerT5D23}
\end{figure}

\begin{table}[h]
  \centering
  \begin{tabular}{l | c c c}
  \toprule
  & $D_\mathrm{KL,+}$ & $D_\mathrm{KL,-}$ & $D_\mathrm{KL}(P_+\|P_-)$ \\
  \midrule
$\xi$      & 1.20 & 1.77 & 5.55 \\
$\delta_1$ & 3.02 & 3.06 & 0.08 \\
$\beta_1$  & 2.93 & 2.93 & 0.94 \\
$\beta_2$  & 2.67 & 2.88 & 0.52 \\
$\beta_3$  & 2.82 & 2.50 & 0.24 \\
  \bottomrule
  \end{tabular}
  \caption{Yang--Mills--PGT (Bahamonde base): per-coupling $D_\mathrm{KL}$
    carrier identification for the plane-wave-benchmark survey
    (\cref{CornerT5D20} shows the top-$K$ carrier parameters).}
  \label{KLTableYMBahamonde}
\end{table}

\begin{table}[h]
  \centering
  \begin{tabular}{l | c c c}
  \toprule
  & $D_\mathrm{KL,+}$ & $D_\mathrm{KL,-}$ & $D_\mathrm{KL}(P_+\|P_-)$ \\
  \midrule
$\xi$      & 2.65 & 1.59 & 4.87 \\
$\chi$     & 3.00 & 3.11 & 0.64 \\
$\delta_1$ & 3.09 & 2.98 & 0.52 \\
$\beta_3$  & 2.97 & 2.55 & 0.22 \\
$\beta_1$  & 2.36 & 2.83 & 1.30 \\
  \bottomrule
  \end{tabular}
  \caption{Yang--Mills--PGT (Barker extension): per-coupling $D_\mathrm{KL}$
    carrier identification for the plane-wave-benchmark survey
    (\cref{CornerT5D21} shows the top-$K$ carrier parameters).}
  \label{KLTableYMBarker}
\end{table}

\begin{table}[h]
  \centering
  \begin{tabular}{l | c c c}
  \toprule
  & $D_\mathrm{KL,+}$ & $D_\mathrm{KL,-}$ & $D_\mathrm{KL}(P_+\|P_-)$ \\
  \midrule
$\beta_1$  & 2.37 & 3.04 & 4.77 \\
$\zeta_2$  & 2.96 & 2.97 & 0.16 \\
$\delta_1$ & 2.87 & 2.94 & 0.20 \\
$\beta_2$  & 2.48 & 2.83 & 1.14 \\
$\zeta_3$  & 2.63 & 2.80 & 0.25 \\
  \bottomrule
  \end{tabular}
  \caption{Yang--Mills--PGT (Shapiro extension): per-coupling $D_\mathrm{KL}$
    carrier identification for the plane-wave-benchmark survey
    (\cref{CornerT5D22} shows the top-$K$ carrier parameters).}
  \label{KLTableYMShapiro}
\end{table}

\begin{table}[h]
  \centering
  \begin{tabular}{l | c c c}
  \toprule
  & $D_\mathrm{KL,+}$ & $D_\mathrm{KL,-}$ & $D_\mathrm{KL}(P_+\|P_-)$ \\
  \midrule
$\chi$     & 2.31 & 3.01 & 3.86 \\
$\delta_1$ & 2.93 & 2.71 & 0.17 \\
$\zeta_2$  & 2.81 & 2.86 & 0.25 \\
$\beta_1$  & 2.56 & 2.80 & 0.77 \\
$\zeta_3$  & 2.71 & 2.72 & 0.27 \\
  \bottomrule
  \end{tabular}
  \caption{Yang--Mills--PGT (non-minimal union): per-coupling $D_\mathrm{KL}$
    carrier identification for the plane-wave-benchmark survey
    (\cref{CornerT5D23} shows the top-$K$ carrier parameters).}
  \label{KLTableYMFull}
\end{table}

% Discussion preserved (planning directive 2026-05-25):
% Rescue logic: the v1 chain at t_end=10 saturated the modal-solver
% expm-overflow ceiling (max_eigenvalue × t_end > 708) at the
% eighteen-dimensional prior. Re-running at t_end=1 stays under the
% ceiling; A(θ) is t_end-independent in the perturbative regime
% (cross-ref App G \cref{PerturbativityHierarchy}), so the rescue is
% physically equivalent to the v1 target up to numerical precision.
% The +13.48 nat Bayes factor is the survey maximum, ~6 nats above the
% full nine-parameter non-minimal sector.
\subsubsection{Non-propagating-torsion control}
The amplifying parameter regions identified above produce exponential
mode growth in the linearised plane-wave evolution: at every such point
the conversion grows because some mode in the linearised system has
positive $\mathrm{Re}\,\lambda$. Whether such growth signals a
genuine new amplification mechanism, a non-ghost tachyonic
instability of the unbounded plane-wave ansatz, or a wrong-sign
kinetic-mode ghost that would render the theory non-unitary is not
in general decidable from the chains alone. The set of torsion modes
that propagate, and whether any of them carries the wrong sign,
depends on the term subset and the
parameter values~\cite{sezgin1980ghostfree,blagojevic2018hamiltonian,lin2019pgt,Yo:1999ex,Yo:2001sy};
robust resolution requires the full Hamiltonian particle-spectrum
analysis carried out by the \PSALTer
tool~\cite{barker2024psalter,barker2025psalter2}, integration with
which we defer to future work.

To bound the ghost branch from below \emph{without} \PSALTer we run a
control test: the same Yang--Mills--PGT non-minimal sector with the
torsion kinetic coefficient $\xi$ pinned to zero. The Lagrangian then
introduces no new kinetic operator for any field beyond the
Einstein--Maxwell baseline; torsion is determined algebraically by
the Cartan equation. The no-ghost classification is trivially
satisfied --- there is no kinetic operator whose sign needs checking.
Informed by the $\Del{1}$ sector result above (where switching
on a non-propagating vertex produced suppression-dominated structure
on narrower prior support) we expected the control to collapse the
amplification corner. Instead nested sampling gives
\begin{equation*}
  \log\mathcal{Z}_+ = +10.98 \pm 0.15
  \qand
  \log\mathcal{Z}_- = +2.67 \pm 0.10,
\end{equation*}
($\log B = +8.31$), essentially
reproducing the propagating-torsion result.

\paragraph*{$\chi$-closure control ($\xi=0$, 17 parameters)}
Repeating the test at the seventeen-parameter $\chi$-closure sub-space
$\{\Bet{1\!-\!3}, \Del{1}, \zeta_{1\!-\!3}, \chi_{1\!-\!10}\}$ with
$\xi = 0$ returns
\begin{equation*}
  \log\mathcal{Z}_+ = +9.93 \pm 0.08 \, , \quad
  \log\mathcal{Z}_- = -3.28 \pm 0.07 \, ,
\end{equation*}
($\log B = +13.21$), within the
bootstrap uncertainty of the propagating $\chi$-closure chain. The
amplification therefore does not require torsion to propagate; the
non-minimal torsion--electromagnetism vertices alone reproduce the
amp-vs-sup structure of the propagating theory. Per-parameter
$D_\mathrm{KL}$ (tables below) resolves the carrier:
$\Bet{1}$ dominates the eight-parameter control
($D_\mathrm{KL}(P_+\|P_-) = 3.5$ nats), and the seventeen-parameter
$\chi$-closure control returns $\chi_1$ as the dominant carrier
($D_\mathrm{KL}(P_+\|P_-) = 5.8$ nats), with the explicit
contraction $\rcD{^\alpha}\mathcal{T}^{\beta\gamma\delta}\,
\tilde{\mathcal{R}}_{\alpha\beta\gamma\delta}$.

\paragraph*{Interpretation of exponential mode growth}
The pipeline accepts these exponentially-growing modes through the
modal-solver evolution by design (\cref{InferenceArchitecture}):
prejudging which mode behaviours are unphysical for an input theory
is not the role of a theory-agnostic survey. The remaining
interpretive question is whether the growth represents a physical
amplification --- which a finite-extent geometry would saturate while
preserving channel mixing --- or a non-ghost tachyonic-instability
artefact of the unbounded plane-wave ansatz. Several precedents in
the literature show that linearised exponential / tachyonic modes
need not be fatal pathologies. In the Standard Model the Higgs field
appears tachyonic about the unbroken vacuum but is perfectly unitary
about its symmetry-broken ground
state~\cite{legner2025torc}; analogously, in PGT the spin-connection
modes can be massive in the ultraviolet and integrate out at the
infrared to recover general relativity as the low-energy effective
theory, so linearised tachyons can be a signature of the wrong
vacuum rather than of a fatal pathology. Higher-derivative gravity
theories that exhibit Ostrogradsky ghosts at the linearised level
can be non-linearly ghost-free when appropriate
torsion or higher-curvature
terms are present~\cite{aoki2020nonlinear}. Recent metric-affine
catalogues show that ghost-tachyon tunings constructed at the
linearised level break down nonlinearly, and that the only
unitarity-protected branches are those where a gauge symmetry rather
than a parameter choice removes the ghost~\cite{barker2025irfound1,barker2025irfound2};
this is precisely the branch that the non-propagating-torsion control
sits in. These precedents do not prove that the linearised
exponential growth we observe is physical; they establish that it is
not automatically fatal, and that resolution belongs to either the
finite-extent geometry of \cref{Discussion} or to a future
\PSALTer-based particle-spectrum analysis.

A small set of supporting diagnostics is consistent with the
channel-mixing interpretation in the non-propagating-torsion sector:
the algebraic torsion field remains bounded relative to the
metric/photon perturbations at the maximum-a-posteriori point of the
chain, ruling out a constraint-divergence pathology; the linearised
eigenvalue spectrum at that point shows no
$\mathrm{Re}(\lambda) > 0$ modes in the pure metric channel; the
energy-conservation audit at the non-propagating samples sits at the
modal-solver precision floor (\cref{ConservationAudit}); and the
perturbativity envelope of \cref{PerturbativeBound} is satisfied
across the posterior support. The carrier sub-corners are
\cref{CornerNP} (eight-parameter control) and \cref{CornerNPCeven}
(seventeen-parameter $\chi$-closure control).

\begin{figure}[t]
  \centering
  \includegraphics[width=\columnwidth]{figures/kl_carrier_corner_np_t5}
  \caption{Carrier sub-corner for the eight-dimensional
    non-propagating-torsion control sector. The torsion kinetic
    coefficient $\xi$ is pinned to zero relative to the full
    nine-parameter non-minimal sector of \cref{CornerT5D23}, leaving
    the torsion field algebraically determined by the Cartan
    equation. The top-$K$ parameters ranked by combined marginal-amp
    / marginal-sup / cross-amp-sup $D_\mathrm{KL}$ are plotted.}
  \label{CornerNP}
\end{figure}

\begin{table}[h]
  \centering
  \begin{tabular}{l | c c c}
  \toprule
  & $D_\mathrm{KL,+}$ & $D_\mathrm{KL,-}$ & $D_\mathrm{KL}(P_+\|P_-)$ \\
  \midrule
$\beta_1$  & 2.19 & 2.68 & 3.48 \\
$\zeta_2$  & 2.99 & 2.05 & 0.74 \\
$\chi$     & 2.81 & 2.89 & 0.57 \\
$\zeta_1$  & 2.82 & 2.56 & 0.23 \\
$\delta_1$ & 2.76 & 2.73 & 0.26 \\
  \bottomrule
  \end{tabular}
  \caption{Non-propagating torsion ($\xi=0$ control): per-coupling $D_\mathrm{KL}$
    carrier identification for the plane-wave-benchmark survey
    (\cref{CornerNP} shows the top-$K$ carrier parameters).}
  \label{KLTableNP}
\end{table}

\begin{figure}[t]
  \centering
  \includegraphics[width=\columnwidth]{figures/kl_carrier_corner_np_chi_closure}
  \caption{Carrier sub-corner for the seventeen-dimensional
    non-propagating-torsion $\chi$-closure control. The torsion
    kinetic coefficient $\xi$ is pinned to zero relative to the
    eighteen-parameter $\chi$-closure of \cref{CarrierChiClosure};
    the carrier-parameter structure essentially reproduces the
    propagating result.}
  \label{CornerNPCeven}
\end{figure}

\begin{table}[h]
  \centering
  \begin{tabular}{l | c c c}
  \toprule
  & $D_\mathrm{KL,+}$ & $D_\mathrm{KL,-}$ & $D_\mathrm{KL}(P_+\|P_-)$ \\
  \midrule
$\chi_{1}$  & 1.72 & 2.82 & 5.78 \\
$\chi_{7}$  & 2.37 & 2.80 & 1.44 \\
$\chi_{2}$  & 2.39 & 2.78 & 0.89 \\
$\beta_1$   & 2.60 & 2.76 & 0.57 \\
$\chi_{10}$ & 2.72 & 2.54 & 0.29 \\
$\chi_{9}$  & 2.55 & 2.70 & 0.49 \\
$\beta_2$   & 2.50 & 2.68 & 0.77 \\
$\zeta_1$   & 2.68 & 2.51 & 0.31 \\
$\chi_{5}$  & 2.68 & 2.63 & 0.27 \\
$\zeta_2$   & 2.62 & 2.51 & 0.38 \\
  \bottomrule
  \end{tabular}
  \caption{Non-propagating torsion ($\xi=0$, $\chi$-closure control): per-coupling $D_\mathrm{KL}$
    carrier identification for the plane-wave-benchmark survey
    (\cref{CornerNPCeven} shows the top-$K$ carrier parameters).}
  \label{KLTableNPChiClosure}
\end{table}

\subsubsection{The complete parity-even sector ($\chi$-closure)}
The complete parity-even sector closes the
$\tilde{\mathcal{R}}\times\nabla\mathcal{T}$ basis of
\cref{DerivativeExtensions} beyond Barker's single derivative-mixing
parameter by retaining the full ten-parameter $\chi_{1\!-\!10}$
sector on top of the Yang--Mills--PGT non-minimal
base~\cite{barker2024poincare},
\begin{align}
  \lag_\chi
    = \lag_\text{YM--PGT}\bigl|_{\chi\to 0}
       + \sum_{i=1}^{10}\chi_i\,
        \tilde{\mathcal{R}}\!\times\!\nabla\mathcal{T}\bigr|_{i} \, ,
  \label{ChiClosureLagrangian}
\end{align}
giving an eighteen-dimensional prior. Nested sampling gives
\begin{equation*}
  \log\mathcal{Z}_+ = +10.30 \pm 0.08
  \qand
  \log\mathcal{Z}_- = -3.18 \pm 0.07,
\end{equation*}
($\log B = +13.48$) under the
plane-wave approximation: the amplification log-evidence is comparable
to the nine-parameter non-minimal union, while the suppression
log-evidence drops below zero, indicating that closing the
$\chi_i$ basis tightens the constraint that suppression is confined
to small $\chi$ rather than opening additional suppression
corners. Per-parameter $D_\mathrm{KL}$ (\cref{KLTableChiClosure})
resolves the carrier structure further: $\chi_1$ alone accounts for
$D_\mathrm{KL}(P_+\|P_-) = 4.2$ nats --- five times larger than any
other coupling --- identifying the explicit contraction
$\rcD{^\alpha}\mathcal{T}^{\beta\gamma\delta}\,
\tilde{\mathcal{R}}_{\alpha\beta\gamma\delta}$ as the operator that
drives the parity-even amp-vs-sup distinction. The
carrier sub-corner is \cref{CarrierChiClosure}.

\begin{figure}[t]
  \centering
  \includegraphics[width=\columnwidth]{figures/kl_carrier_corner_chi_closure}
  \caption{Carrier sub-corner for the eighteen-dimensional
    $\chi$-closure: the six parameters ranked highest by combined
    marginal-amp / marginal-sup / cross-amp-sup $D_\mathrm{KL}$.  The
    amplification (magenta) and suppression (yellow) marginals separate
    most strongly along $\chi_1$, the
    $\rcD{^\alpha}\mathcal{T}^{\beta\gamma\delta}
     \,\tilde{\mathcal{R}}_{\alpha\beta\gamma\delta}$ contraction.
    full $D_\mathrm{KL}$ carrier ranking is in \cref{KLTableChiClosure}.}
  \label{CarrierChiClosure}
\end{figure}
\label{CornerT7}% legacy compatibility — the CarrierChiClosure figure supersedes the previous full-corner figure

\begin{table}[h]
  \centering
  \begin{tabular}{l | c c c}
  \toprule
  & $D_\mathrm{KL,+}$ & $D_\mathrm{KL,-}$ & $D_\mathrm{KL}(P_+\|P_-)$ \\
  \midrule
$\chi_{1}$  & 1.68 & 2.82 & 4.18 \\
$\chi_{7}$  & 2.38 & 2.81 & 0.66 \\
$\chi_{5}$  & 2.74 & 2.55 & 0.22 \\
$\beta_1$   & 2.59 & 2.73 & 0.52 \\
$\beta_2$   & 2.67 & 2.73 & 0.46 \\
$\zeta_1$   & 2.72 & 2.60 & 0.35 \\
$\chi_{9}$  & 2.44 & 2.70 & 0.78 \\
$\chi_{6}$  & 2.50 & 2.68 & 0.30 \\
$\beta_3$   & 2.68 & 2.61 & 0.40 \\
$\chi_{2}$  & 2.37 & 2.66 & 0.85 \\
  \bottomrule
  \end{tabular}
  \caption{Complete parity-even ($\chi$-closure) sector: per-coupling $D_\mathrm{KL}$
    carrier identification for the plane-wave-benchmark survey
    (\cref{CarrierChiClosure} shows the top-$K$ carrier parameters).}
  \label{KLTableChiClosure}
\end{table}


% Discussion preserved (planning directive 2026-05-25):
% T9 v2 partial-rescue caveat: both log Z negative (+10.30 amp vs
% −0.18 sup at the FULL 32D ξ-closure; here the amplification half
% nominally returns −0.18 ± 0.07 and the suppression half −9.61 ± 0.09).
% Partial floor contamination from the extended ξ₁₁–ξ₁₆ sub-sector
% (which sits beyond Barker's single ξ at the edge of the t_end=1
% overflow window) means the +9.43 nat Bayes factor is a LOWER BOUND
% on the genuine amplification preference of the parity-even kinetic
% closure. T9 closes the ξ₁–ξ₁₆ kinetic-invariant basis (Barker's
% single ξ = LC{ξ_6, ξ_7, ξ_{12}, ξ_{16}}) so the kinetic-closure
% Bayes factor is structural rather than a scope artefact.
\subsubsection{The $\xi$-kinetic closure}
The $\xi$-kinetic closure replaces the single torsion kinetic
coefficient $\xi$~\cite{bahamonde2025torsion} --- which equals
$\mathrm{LC}\{\xi_6, \xi_7, \xi_{12}, \xi_{16}\}$ of the complete
basis (\cref{DerivativeExtensions}) --- with the full
sixteen-parameter $\xi_{1\!-\!16}$ basis of the $(\nabla\mathcal{T})^2$
class (\cref{DerivativeExtensions}), on top of the complete
$\chi_{1\!-\!10}$
$\tilde{\mathcal{R}}\!\times\!\nabla\mathcal{T}$ basis. One
invariant, $\xi_{11}$, vanishes in the plane-wave reduction; fifteen
propagating $\xi$-couplings plus the ten $\chi$-couplings and the
seven non-minimal-base couplings give a thirty-two-dimensional prior.
Nested sampling gives
\begin{equation*}
  \log\mathcal{Z}_+ = -0.18 \pm 0.07
  \qand
  \log\mathcal{Z}_- = -9.61 \pm 0.09,
\end{equation*}
($\log B = +9.43$); both
$\log\mathcal{Z}$ are negative, indicating partial floor contamination
from the extended $\xi_{11\!-\!16}$ sub-sector, so
the $+9.43$ nat Bayes factor is a lower bound on the genuine
amplification of the parity-even kinetic closure under the
plane-wave approximation. Per-parameter $D_\mathrm{KL}$ (\cref{KLTableXiClosure}) again identifies
$\chi_1$ as the dominant amp/sup carrier ($D_\mathrm{KL}(P_+\|P_-) = 4.4$ nats),
with the strongest $\xi$-direction contribution arriving through $\xi_{15}$
($D_\mathrm{KL}(P_+\|P_-) = 3.5$ nats), whose explicit contraction
is $\rcD{^\alpha}\mathcal{T}^{\beta}{}_\alpha{}^\gamma\,
\rcD{^\delta}\mathcal{T}_{\gamma\beta\delta}$.

\begin{figure}[t]
  \centering
  \includegraphics[width=\columnwidth]{figures/kl_carrier_corner_xi_kinetic_closure}
  \caption{Carrier sub-corner for the thirty-two-dimensional
    $\xi$-kinetic closure. The top-$K$ parameters by combined
    marginal-amp / marginal-sup / cross-amp-sup $D_\mathrm{KL}$ are
    plotted; full $D_\mathrm{KL}$ carrier ranking in \cref{KLTableXiClosure}.}
  \label{CornerT9}
\end{figure}

\begin{table}[h]
  \centering
  \begin{tabular}{l | c c c}
  \toprule
  & $D_\mathrm{KL,+}$ & $D_\mathrm{KL,-}$ & $D_\mathrm{KL}(P_+\|P_-)$ \\
  \midrule
$\chi_{1}$  & 2.02 & 2.73 & 4.40 \\
$\xi_{15}$  & 2.47 & 2.59 & 3.47 \\
$\chi_{8}$  & 2.92 & 2.58 & 0.54 \\
$\chi_{7}$  & 2.39 & 2.91 & 2.40 \\
$\beta_1$   & 2.89 & 2.63 & 0.60 \\
$\xi_{8}$   & 2.87 & 2.30 & 0.50 \\
$\xi_{14}$  & 2.86 & 2.45 & 0.75 \\
$\xi_{9}$   & 2.86 & 2.47 & 1.36 \\
$\xi_{7}$   & 2.81 & 2.63 & 0.69 \\
$\xi_{6}$   & 2.81 & 2.54 & 0.73 \\
  \bottomrule
  \end{tabular}
  \caption{$\xi$-kinetic closure sector: per-coupling $D_\mathrm{KL}$
    carrier identification for the plane-wave-benchmark survey
    (\cref{CornerT9} shows the top-$K$ carrier parameters).}
  \label{KLTableXiClosure}
\end{table}

Beyond the PGT geometric hierarchy, the \TIDAL{} pipeline's
theory-agnostic architecture admits any Lagrangian operator combination
as input; two phenomenologically motivated survey axes probe whether
the conversion signals observed above could arise from effective
interactions outside the PGT framework.
The dark-photon-plasma analogue asks whether a Proca-like torsion mass
term can reproduce the amplification signal, bridging from the PGT
geometric language to the effective dark-photon phenomenology
of~\cite{caputo2021darkphoton}.
A parallel QED-EFT axis along the Euler--Heisenberg $F^4$ corrections
tests whether the amplification could be an artefact of nonlinear
electromagnetic corrections at the surveyed field strengths.
The full and complete parity-odd sectors exhibit plane-wave overflow
at any viable propagation time; this is discussed in
\cref{ParityOddGeomIncomp} below.

% Discussion preserved (planning directive 2026-05-25):
% Eigenvalue numerics:
%   - T6 (parity-odd full, 20D, 29687506) at t_end=1: max Re λ ≈ 1.4e3,
%     ~2× the exp(708) modal-solver overflow ceiling. Would need
%     t_end ≈ 1/2 to clear the ceiling.
%   - T8 (complete parity-odd, 53D, 29684804) at t_end=1: max Re λ
%     ≈ 5.5e5; would need t_end ≈ 0.0013 at which
%     P ∝ (κB₀ t/2)² ≈ 2.5e-17 falls many orders below the soft floor.
% This is STRUCTURAL geometric incompatibility (parity-odd ε-tensor-
% mediated T–F and \tilde{R}–∇T couplings generate exponential mode
% amplification on the unbounded plane-wave background), NOT a
% numerical-precision failure. The bounded interaction region of a
% finite-extent geometry would saturate the growth without removing
% the channel-mixing physics.
% No T6/T8 corner figure is published: the visible posterior structure
% in any corner would reflect the geometry of the overflow threshold
% (max_eigenvalue × t_end < 708) in coupling space rather than a
% physical stability boundary, and would visually conflate the
% overflow-threshold artefact with the parity-even posterior content.
\subsubsection{Parity-odd geometric incompatibility}%
\label{ParityOddGeomIncomp}
The two parity-odd torsion--electromagnetism coupling sectors --- the
twenty-parameter parity-odd full sector
$\{d_{14}, d_{15}, d_{17}, d_{19\!-\!21},
\tilde\zeta_{1,2,3,5,6}\}$ on top of the Yang--Mills--PGT non-minimal
base, and the fifty-three-parameter complete parity-odd closure that
additionally retains the
$\varepsilon\!\cdot\!\tilde{\mathcal{R}}\!\times\!\nabla\mathcal{T}$
basis $\{\tilde\chi_1, \ldots, \tilde\chi_{36}\}$ on top of the
parity-odd-full base --- fail to admit a finite-amplitude posterior
under the plane-wave uniform-$\Bzero$ ansatz at any horizon. The
eigenvalues of the linearised conversion system grow unbounded in
these coupling directions: they reach above
the modal-solver overflow ceiling at
$t_\text{end} = 1$. The would-be
safe horizon for the latter is $t_\text{end} \lesssim 10^{-3}$, at
which the Gertsenshtein conversion amplitude
$P \propto (\kappa\Bzero t/2)^2$ drops below the noise
floor by orders of magnitude. The parity-odd full sector returns
$\log\mathcal{Z}_\pm \approx -15$ with $\log B \approx 0$, the
soft-penalty noise signature, and the complete parity-odd closure
returns zero dead points within the available compute. The structural
origin is geometric: the parity-odd
$\varepsilon$-tensor-mediated contractions
$\varepsilon\!\cdot\!\mathcal{T}\!\cdot\!\mathcal{F}$ and
$\varepsilon\!\cdot\!\tilde{\mathcal{R}}\!\cdot\!\nabla\mathcal{T}$
generate exponential mode amplification in the unbounded plane-wave
background --- a pathology the bounded interaction time of a
finite-extent geometry (\cref{Discussion}) would not permit.

\begin{figure}[t]
  \centering
  \includegraphics[width=\columnwidth]{figures/kl_carrier_corner_parity_odd_full}
  \caption{Carrier sub-corner for the twenty-dimensional parity-odd
    full sector. The amplification and suppression marginals are
    coincident across the carrier parameters, the soft-floor
    signature of the geometric incompatibility discussed in the
    text; nested sampling gives $\log\mathcal{Z}_\pm \approx -15$.}
  \label{CornerParityOddFull}
\end{figure}

\subsubsection{Dark-photon plasma }
The dark-photon-plasma analogue probes a distinct survey axis from
the PGT extensions above: the trace-vector torsion $\T[2]{_\mu}$ is
promoted to a Proca field with kinetic mixing to the photon and a
plasma-mass coupling, recovering the standard dark-photon
phenomenology of~\cite{caputo2021darkphoton} at $\Bzero \to 0$. The
bare kinetic-mixing model alone produced no conversion signal: the
modal eigendecomposition of the linearised system has a degeneracy
between the photon mode and the Proca-trace mode that closes the
mixing channel at leading order, so the inference returns the
Einstein--Maxwell baseline regardless of the kinetic-mixing strength.
We therefore added the Proca mass $m_A^2$ to lift the degeneracy and
open the mixing channel, with detuning coupling $\delta_m$
controlling the deviation of the photon mode from the
Proca-trace mode,
\begin{widetext}
\begin{align}
  \lag_\text{dp}
    = -\tfrac{1}{\kappa^2}\,\MAGFt{}
      + \Bet{3} I_3
      - \tfrac{1}{4}\FieldF{_{\mu\nu}}\FieldF{^{\mu\nu}}
      - \tfrac{1}{2}m_A^2\,\T[2]{_\mu}\T[2]{^\mu}
      + \xi\,\T[2]{_\mu}\PD{_\nu}\FieldF{^{\mu\nu}}
      + \delta_m\,\T[2]{_\mu}\T[2]{^\mu}\,
              \FieldF{_{\alpha\beta}}\FieldF{^{\alpha\beta}} \, ,
  \label{DarkPhotonPlasmaLagrangian}
\end{align}
\end{widetext}
giving a four-dimensional prior over
$\{m_A^2, \xi, \delta_m, \Bet{3}\}$. Nested sampling gives
\begin{align}
  \log\mathcal{Z}_+ = +9.03 \pm 0.07 \, , \quad
  \log\mathcal{Z}_- = +2.92 \pm 0.08
\end{align}
($\log B = +6.11$). The
amplification log-evidence dominates the suppression log-evidence,
but per-parameter $D_\mathrm{KL}$ (\cref{KLTableDarkPhoton}) identifies
the plasma-mass coupling $\delta_m$ as the dominant carrier
($D_\mathrm{KL}(P_+\|P_-) = 3.4$~nats); the kinetic mixing $\xi$
adds little structure beyond it. The result is therefore essentially
a Proca-mass-induced photon-mode-shift effect rather than a
torsion-electromagnetism coupling phenomenon, and is largely
orthogonal to the non-minimal PGT extensions of
\cref{InformativeAmplification}.

\begin{figure}[t]
  \centering
  \includegraphics[width=\columnwidth]{figures/kl_carrier_corner_dark_photon_plasma}
  \caption{Carrier sub-corner for the four-dimensional dark-photon
    plasma analogue. The amplification posterior (magenta)
    concentrates at non-zero plasma mass $m_A^2$ and detuning
    $\delta_m$; the suppression posterior (yellow) is broader,
    consistent with the dominant amplification preference.}
  \label{CornerT1}
\end{figure}

\begin{table}[h]
  \centering
  \begin{tabular}{l | c c c}
  \toprule
  & $D_\mathrm{KL,+}$ & $D_\mathrm{KL,-}$ & $D_\mathrm{KL}(P_+\|P_-)$ \\
  \midrule
  $\delta_m$    & 2.77 & 2.98 & 3.38 \\
  $m_A^{2}$    & 0.50 & 1.25 & 1.45 \\
  $\xi$         & 0.85 & 1.24 & 0.00 \\
  $\alpha_3$    & 0.66 & 0.67 & 0.49 \\
  \bottomrule
  \end{tabular}
  \caption{Dark-photon plasma analogue: per-coupling $D_\mathrm{KL}$
    carrier identification for the plane-wave-benchmark survey
    (\cref{CornerT1} shows the top-$K$ carrier parameters).}
  \label{KLTableDarkPhoton}
\end{table}

% Discussion preserved (planning directive 2026-05-25):
% EH+Gert is a parallel survey axis (QED-EFT F⁴ corrections to
% Einstein–Maxwell), NOT a PGT extension. Two physical couplings:
%   ρ — coefficient of (F·F)²
%   σ — coefficient of (F·F̃)²
% QED reference point: σ/ρ = 7/16 (Adler 1971, Dunne 2012).
%
% The single F·F̃ Pontryagin topological term is correctly identified
% as a total derivative at the symbolic level by the Wolfram E-L
% pipeline (gertsenshtein_eh_top.json: lambdatop absent from the EOM,
% 27 H-terms identical to the base EH derivation). The
% symbolic-derivation outcome is the publishable validation — running
% PolyChord over an extended (ρ, σ, λ_top) prior would only sample
% the prior in the λ_top direction at HPC cost and tell us nothing
% the derivation already shows.
%
% EH+Gert + NP together bracket the genuine-amplification
% interpretation: NP rules out the torsion-propagation artefact;
% EH+Gert rules out the QED-EFT artefact (we did not unconsciously
% import an EM-EFT correction that mimics gravitational amplification).
\subsubsection{Euler--Heisenberg}
A parallel survey axis along the QED-effective-field-theory $F^4$
corrections to Einstein--Maxwell retains the two physical couplings
of~\cite{adler1971photon,dunne2004heisenberg},
\begin{align}
  \lag_\text{EH}
    = -\tfrac{1}{\kappa^2}\,\MAGFt{}
       - \tfrac{1}{4}\FieldF{_{\mu\nu}}\FieldF{^{\mu\nu}}
    + \lambda_\text{top}\,\FieldF{_{\mu\nu}}\FieldFtilde{^{\mu\nu}}
    + \rho\,(\FieldF{_{\mu\nu}}\FieldF{^{\mu\nu}})^{2}
    + \sigma\,(\FieldF{_{\mu\nu}}\FieldFtilde{^{\mu\nu}})^{2} \, ,
  \label{EHGertLagrangian}
\end{align}
giving a two-dimensional prior. Nested sampling gives
\begin{align}
  \log\mathcal{Z}_+ = +0.184 \pm 0.001 \, , \quad
  \log\mathcal{Z}_- = -0.185 \pm 0.0004 \, , \quad
  \log B = +0.37 \, .
\end{align}

a symmetric and tight null, with the QED reference point
$\sigma/\rho = 7/16$~\cite{adler1971photon,dunne2004heisenberg}
indistinguishable from the Einstein--Maxwell baseline at the surveyed
$\Bzero = 10^{-2}$. The single $F\cdot\tilde F$ Pontryagin topological
term is correctly identified as a total derivative at the symbolic
level by the Euler--Lagrange derivation pipeline
(\cref{SymbolicEulerHeisenberg}).  The per-parameter $D_\mathrm{KL}$ (\cref{KLTableEH})
yields $D_\mathrm{KL}(P_+\|P_-) < 1.7$ nats for both couplings,
consistent with the null verdict.

\begin{figure}[t]
  \centering
  \includegraphics[width=\columnwidth]{figures/kl_carrier_corner_eh_gertsenshtein}
  \caption{Overlaid amplification (magenta) and suppression (yellow) corner
    plot for the two-dimensional Euler--Heisenberg $F^4$ extension
    of Einstein--Maxwell, in the couplings $\rho$ of $(F\!\cdot\! F)^2$
    and $\sigma$ of $(F\!\cdot\!\tilde F)^2$. The amplification and
    suppression posteriors are symmetric and tightly localised about
    the origin, consistent with the QED reference point
    $\sigma/\rho = 7/16$~\cite{adler1971photon,dunne2004heisenberg}
    being indistinguishable from the Einstein--Maxwell baseline at
    $\Bzero = 10^{-2}$.}
  \label{CornerEHGert}
\end{figure}

\begin{table}[h]
  \centering
  \begin{tabular}{l | c c c}
  \toprule
  & $D_\mathrm{KL,+}$ & $D_\mathrm{KL,-}$ & $D_\mathrm{KL}(P_+\|P_-)$ \\
  \midrule
  $\rho$  & 2.64 & 2.70 & 0.45 \\
  $\sigma$ & 2.41 & 2.64 & 1.66 \\
  \bottomrule
  \end{tabular}
  \caption{Euler--Heisenberg sector: per-coupling $D_\mathrm{KL}$
    carrier identification for the plane-wave-benchmark survey
    (\cref{CornerEHGert} shows the carrier parameters).}
  \label{KLTableEH}
\end{table}