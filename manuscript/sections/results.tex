% Results scaffold (Phase 2): survey-shaped structure that does not commit
% to specific findings at scaffold time, since several HPC campaigns are
% still in progress and the eventual headline finding cannot be predicted
% with confidence.
%
% Structure (target ~1900 w):
%   §4 opens with a structural map and a "Survey perimeter" paragraph
%      declaring which PGT theory classes were scanned and the parameter
%      dimensions of each. This paragraph is the load-bearing scope
%      statement that anchors every subsequent subsection.
%   §4.1 Validation: Boccaletti reproduction (~250 w + figure 1).
%   §4.2 Sector survey: a paired-Bayes-factor table summarising the
%        outcome of each scanned theory in a single subsection. As more
%        campaigns finish, rows are added; the subsection structure does
%        not change. (~600 w + figures D.1, D.2 cross-refs).
%   §4.3 Channel structure: hx↔ax torsion-independence as the kinematic
%        identity that explains the survey nulls — repositioned as a
%        mechanism, not a headline (~400 w + figure 2).
%   §4.4 Headline finding (TBD): a deliberately flexible slot for whichever
%        finding emerges as most striking from the in-progress campaigns —
%        could be an amplification corner, a new instability boundary, or
%        a structural feature in the symbolic outputs (~600 w).
%
% Rationale: this structure can absorb new campaign results without
% restructuring. Nulls feed §4.2 (just a new table row); positive findings
% feed §4.4 (the headline slot). The single-point ghost diagnosis that the
% original scaffold had as §4.5 is moved to App D as methodology validation
% per the structural-review recommendations.

\section{Results}%
\label{Results}

% Opening structural map. One sentence, four-item enumeration matching
% the four subsections.
% TODO: "§4.1 reports the validation against the Boccaletti formula;
% §4.2 summarises the sector survey across the [N] PGT theory classes
% we scanned; §4.3 isolates the kinematic channel structure that
% underlies the survey nulls; §4.4 reports the headline finding."

% NEW — Survey perimeter paragraph. This is the scope-statement anchor
% that names the surveyed theory classes and parameter dimensions.
% Without it, every subsection's scope is implicit and hard to defend.
% Update at drafting time to match the campaigns that have actually
% completed.
\paragraph*{Survey perimeter}
% TODO: list the PGT theory classes scanned (e.g. minimal Einstein–Cartan
% with T2 coupling, minimal-coupling dark-photon-plasma sector, propagating
% PGT in a chosen ghost-free subspace, nonminimal Lagrangians with curvature
% –EM cross-terms, …). For each class, state the parameter dimension of
% the prior and the order of magnitude of the parameter range. Cross-ref
% \cref{Validation} (App D) for the validation hierarchy and \cref{HPC}
% if HPC infrastructure detail is appendix-resident.

\subsection{Validation against the Einstein--Maxwell baseline}%
\label{ValidationResult}
% Target ~250 w. Leads with the formula; figure 1 placeholder; quantitative
% agreement filled at drafting time from the final Boccaletti validation
% campaign.

\paragraph*{Validation setup}
% TODO: state the validation point ($\Bzero$, $D$, $t_\text{end}$, grid
% resolution); cross-ref \cref{GertsenshteinFormula}.

\paragraph*{Reproduction of the Boccaletti kernel}
% TODO: report the measured agreement quantitatively (TBD: percentage
% from final HPC run); frame as a physics result (the Gertsenshtein
% kernel is correctly reproduced), not a software test.

% Figure 1 — Boccaletti validation.
\begin{figure}[t]
  \centering
  % TODO: \includegraphics{fig1_boccaletti_validation}
  % TODO caption (per STYLE_GUIDE.md §(h)): "Simulated vs analytic
  % conversion probability $\Pconv(t)$ for Einstein–Maxwell on a flat
  % Minkowski background with uniform $\Bzero = [\text{value}]$ and
  % propagation distance $D = [\text{value}]$. Upper panel: simulated
  % (solid) and analytic (dashed); lower panel: residual. The maximum
  % relative deviation across the validation grid is $[\text{value}]\%$,
  % demonstrating that the TIDAL pipeline reproduces the Boccaletti
  % kernel."
  \caption{TODO: Boccaletti validation; sim vs analytic with residual.}
  \label{BoccalettiValidation}
\end{figure}

\subsection{Sector survey: scanned theories and outcomes}%
\label{SectorSurvey}
% TODO: add a sentence noting that no-tachyon and no-ghost conditions from
% \cite{sezgin1980ghostfree,blagojevic2018hamiltonian,aoki2020nonlinear,barker2021hamiltonian}
% were used to define the prior region of the parameter scan below.
% Target ~600 w. Single subsection consolidating the per-sector results
% into a paired-Bayes-factor table — the BHL convention for survey-style
% results sections (compare 2406.12826 §V parameter-sweep tables). New
% campaigns that finish before submission slot in as additional table
% rows, not new subsections.

\paragraph*{Coverage rationale}
% TODO: per Agent 3 review C4 — state the rationale for the swept
% parameter regions. "We sweep [parameters] over [bounds] motivated by
% [physical/literature anchor]; the swept region covers [observational
% target]." Cite \cite{caputo2021darkphoton} for dark-photon parameter
% choices; cite the relevant PGT-stability literature for torsion-coupling
% bounds.

\paragraph*{Summary of scanned theories}
% TODO: introduce the table; state the columns
% (theory / prior dimension / log~Z amplification model / log~Z suppression
% model / D_KL / max~$A$ in the swept region / verdict). Cite the BHL
% precision-bound framing of \cite{lella2024constraining} as the
% template ("we constrain $A < \epsilon$ across [region]").

% Table placeholder for the survey summary.
\begin{table}[t]
  \centering
  % TODO: paired-Bayes-factor table.
  % Columns: Theory | Prior dim | log~Z (amp) | log~Z (sup) | D_KL | max A | Verdict
  % Rows: one per scanned theory class. At Phase 2 scaffold time the
  % theory list is intentionally TBD — fill at drafting time from
  % CAMPAIGN.md and AMPLIFICATION_INVESTIGATION.md as campaigns finish.
  \caption{TODO: paired-Bayes-factor sector survey summary.}
  \label{SectorSurveyTable}
\end{table}

\paragraph*{Mechanism notes}
% TODO: short prose accompanying the table — for each theory class with
% a null verdict, give one sentence on the physical mechanism that
% suppresses amplification at linear order. Cross-ref the relevant App E
% subsection where the symbolic kinetic matrix shows the suppression
% explicitly. Frame as "the [coupling term] vanishes at linear order
% because [reason]" — never "we did not find amplification."

% Cross-references to the App D figures supporting the table.
% (\cref{StageAHeatmap} and \cref{StageBSweep} are defined in App D.)
\paragraph*{Where to read further}
% TODO: one sentence with one-clause hint per STYLE_GUIDE.md §(i):
% "Detailed parameter-plane heatmaps and per-theory sweep figures are in
% \cref{Validation}, with the complete symbolic kinetic matrices archived
% in \cref{SymbolicOutputs}."

\subsection{Observed channel structure across the surveyed theories}%
\label{ChannelStructure}
% Target ~400 w. Demoted from the previous "structural identity" framing
% — the hx↔ax kinetic-coefficient coincidence is an empirical observation
% in a finite explicit set of theories, on a specific B0 background, with
% specific IC choices. There is no algebraic proof, and several open
% validity questions (IC-choice not WLOG; TT-gauge admissibility under
% non-vacuum stress-energy not formally re-derived; theory class is
% finite). This subsection reports the observation honestly and lists
% the caveats; it does NOT claim a theorem.

\paragraph*{Observed pattern in the kinetic matrices}
% TODO: state the empirical observation. In the propagating-PGT theories
% TIDAL has run on, with the IC choices and \Bzero background described
% in \cref{Linearisation}, the hx and ax kinetic-matrix entries coincide
% — the equal-coefficient line is visible in the symbolic output and is
% archived in \cref{HxAxIdentity} (App E.2,
% \cref{SymbolicMechanism}). Cross-ref \cref{KineticMatrix} (§2.2) for
% the linearised matrix in which the coincidence appears. Frame as
% observation, not theorem.

\paragraph*{Mechanism for the surveyed nulls}
% TODO: one paragraph. In the channels and theories where the observed
% pattern holds, the torsion couplings cancel in the linearised
% conversion amplitude, and the prediction reduces to the
% Einstein–Maxwell baseline. This is the kinematic mechanism behind the
% corresponding null entries in \cref{SectorSurveyTable} (§4.2). Frame
% as a partial explanation — only those §4.2 nulls whose theory falls
% within the observation's scope are explained by it.

\paragraph*{Numerical confirmation within the surveyed regime}
% TODO: state the numerical finding ($A = 1$ within $[\text{tolerance}]$
% across the surveyed parameter regions of the propagating-PGT class);
% cross-ref \cref{PropagatingTorsionMap}. The qualifier "within the
% surveyed regime" is essential — the §4.4 headline finding may yet
% identify a non-surveyed regime where the pattern fails.

% New paragraph required by the user-flagged validity concerns. This is
% the most important paragraph in §4.3 — it is the reason §4.3 is in §4
% (Results) and not §2 (Theory).
\paragraph*{Validity caveats}
% TODO: ~80 w. List the open questions that bound the observation's
% scope:
%
%   (a) IC choice not WLOG. The IC family TIDAL has tested is finite;
%   in particular R^2 gravity has additional independent IC sets we
%   have not exercised. A different IC could couple sectors that
%   appear decoupled with our chosen IC.
%
%   (b) TT-gauge legitimacy under \Bzero stress-energy. TT decomposition
%   is gauge-fixed against transverse-traceless modes of vacuum
%   perturbation theory; in the presence of $\Bzero$ stress-energy the
%   residual gauge freedom shrinks, and we have not formally re-derived
%   TT admissibility on this background.
%
%   (c) Theory class is finite. We have not proved extrapolation
%   outside the explicit set of theories TIDAL has run on.
%
% Close with a forward pointer: these caveats motivate the constructive
% paths in \cref{Discussion} (§5).

% Figure 2 — propagating-torsion parameter map. Retained as a
% mechanism-confirming figure within the surveyed regime.
\begin{figure}[t]
  \centering
  % TODO: \includegraphics{fig2_propagating_torsion_map}
  % TODO caption: "Amplification factor $A = \Pconv^\text{torsion} /
  % \Pconv^\text{GR}$ vs the torsion stability parameter $\delta_1$ for
  % the propagating-PGT model at $\Bzero = [\text{value}]$ and
  % $t_\text{end} = [\text{value}]$. $A = 1$ across the surveyed
  % stability window is consistent with the equal-coefficient pattern
  % \cref{HxAxIdentity} observed in the symbolic output."
  \caption{TODO: propagating-torsion parameter map; $A$ vs $\delta_1$.}
  \label{PropagatingTorsionMap}
\end{figure}

\subsection{Headline finding from the survey}%
\label{HeadlineFinding}
% Target ~600 w. Deliberately flexible at scaffold time — the headline
% finding is whichever result emerges as most striking from the
% in-progress HPC campaigns. Could be:
%   (i)  an amplification corner in some theory class (positive signal);
%   (ii) a previously-unknown instability boundary;
%   (iii) a structural feature visible across multiple symbolic outputs;
%   (iv) the closure of a previously-open question in the BHL landscape.
% The drafting session decides which of these is the headline AT
% submission time and writes the corresponding paragraph stubs. Word
% allocation reserves ~600 w so a substantive finding can be developed.

\paragraph*{Setup of the headline regime}
% TODO: name the theory class and parameter regime where the headline
% finding emerges. Briefly describe how this regime was identified by
% the survey (which prior dimension, which campaign, which Bayes-factor
% threshold).

\paragraph*{Result}
% TODO: state the result quantitatively. If positive (e.g. amplification
% corner with non-trivial $A$), give the maximum value and where in
% parameter space it occurs. If negative (e.g. an instability boundary),
% state the boundary equation and how it bounds the physically viable
% subspace. If structural (a kinetic-matrix feature), cross-ref the
% relevant App E equation block.

\paragraph*{Mechanism interpretation}
% TODO: one paragraph linking the headline to the channel structure of
% §4.3 and the survey context of §4.2. Avoid over-claiming generality;
% state precisely the parameter scope ("across the [regime] of the
% [theory class]"); link forward to the discussion's constructive-paths
% framing.

% Headline figure placeholder. Whether this is a parameter-plane plot,
% a time-series, or a kinetic-matrix excerpt depends on what the headline
% turns out to be. Decision deferred to the drafting session.
\begin{figure}[t]
  \centering
  % TODO: include the headline figure once identified. Possible scripts:
  %   - scripts/figures/fig2_propagating_torsion_map.py (reuse if
  %     the headline is a propagating-PGT amplification corner)
  %   - a new fig in scripts/figures/ matching the headline finding's
  %     data source.
  % TODO caption: as per STYLE_GUIDE.md §(h) — what shown, parameter
  % regime, key result.
  \caption{TODO: headline finding figure (figure type TBD).}
  \label{HeadlineFigure}
\end{figure}
