% Outlook -- its own box, placed under the Results box (double-width column).
\begin{block}{Outlook}
  A magnetic field of \emph{finite extent} gives the wave a finite interaction
  time, turning these amplifications into physical conversion magnitudes --- the
  operators ranked here are the inputs that calculation needs. A particle-spectrum
  analysis (\PSALTer{}~[6]) will then classify each amplifying direction.
\end{block}
