\documentclass[border=10pt]{standalone}
\usepackage{tidal-tikz-styles}

\begin{document}
\begin{tikzpicture}[
    node distance=0.4cm and 1.2cm
]

% Entry
\node[stage=Gray] (entry)
    {\textbf{Analytical Jacobian}\\[1pt]
     \scriptsize precomputed $dF/dy + dF/dy'$\\
     \scriptsize (time-independent systems only)};

% D1: size check
\node[decision, below=of entry] (d1)
    {$N \leq 2000$?};

% Dense tier
\node[stage=Green, right=of d1] (dense)
    {\textbf{Dense tier}\\[1pt]
     \scriptsize dense Jacobian callback\\
     \scriptsize \textbf{5.3$\times$ speedup}};

% D2
\node[decision, below=of d1] (d2)
    {$N \leq 200\text{K}$?};

% Sparse tier
\node[stage=Purple, right=of d2] (sparse)
    {\textbf{Sparse tier}\\[1pt]
     \scriptsize CSC sparse Jacobian + SuperLU\\
     \scriptsize \textbf{IDA 2.5$\times$ / CVODE 1.2--1.4$\times$}};
\node[annot, below=0.15cm of sparse]
    {coordinate-format assembly\\(415$\times$ faster than dense)};

% GMRES tier
\node[stage=Blue, below=of d2] (gmres)
    {\textbf{GMRES tier}\\[1pt]
     \scriptsize analytical Jacobian-vector product\\
     \scriptsize matrix-free};

% Arrows
\draw[arr] (entry) -- (d1);
\draw[arr] (d1) -- node[yeslbl, above] {Yes} (dense);
\draw[arr] (d1) -- node[nolbl, left] {No} (d2);
\draw[arr] (d2) -- node[yeslbl, above] {Yes} (sparse);
\draw[arr] (d2) -- node[nolbl, left] {No} (gmres);

% Structure annotation
\node[annot, left=2.0cm of d1, text width=3.4cm] (struct) {%
    \textbf{Jacobian structure:}\\[2pt]
    $dF/dy$: operator coupling\\
    + coefficient gradients\\[2pt]
    $dF/dy'$: kinetic matrix\\
    (identity for field slots,\\
    zero for velocity slots)};
\draw[arr, densely dotted, GrayLine] (struct.east) -- (d1.west);

\end{tikzpicture}
\end{document}
