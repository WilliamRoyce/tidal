\documentclass[border=10pt]{standalone}
\usepackage{tidal-tikz-styles}

\begin{document}
\begin{tikzpicture}[
    source/.style={
        rectangle, rounded corners=3pt, minimum width=2.6cm,
        minimum height=0.9cm, draw=BlueLine, fill=BlueFill,
        fill opacity=0.3, font=\footnotesize\bfseries,
        align=center, text opacity=1
    },
    mtype/.style={
        rectangle, rounded corners=1pt, minimum width=3.0cm,
        minimum height=0.5cm, draw=#1, fill=#1!15,
        font=\scriptsize, align=left, text opacity=1,
        inner sep=3pt
    },
    mtype/.default={GrayLine},
    output/.style={
        rectangle, rounded corners=2pt, minimum width=2.2cm,
        minimum height=0.7cm, draw=GrayLine, fill=GrayFill,
        fill opacity=0.4, font=\footnotesize, align=center,
        text opacity=1
    },
    node distance=0.18cm
]

% Source
\node[source] (snap) {Simulation\\snapshots};

% Dispatcher
\node[font=\footnotesize\bfseries, right=1.8cm of snap] (disp)
    {\texttt{--what}};
\draw[-{Stealth[length=5pt, width=4pt]}, thick, black!70]
    (snap) -- (disp);

% --- Energy group (green) ---
\node[mtype=GreenLine, right=1.4cm of disp, yshift=2.8cm] (m1)
    {\textbf{energy} --- Hamiltonian decomposition};
\node[mtype=GreenLine, below=of m1] (m2)
    {\textbf{conservation} --- $|dE/E_0|$ drift};

% --- Conversion group (purple) ---
\node[mtype=PurpleLine, below=0.35cm of m2] (m3)
    {\textbf{conversion} --- $P(t) = E_{\mathrm{target}}/E_{\mathrm{source}}$};
\node[mtype=PurpleLine, below=of m3] (m4)
    {\textbf{peak\_conversion} --- $\max P(t)$};
\node[mtype=PurpleLine, below=of m4] (m5)
    {\textbf{mixing} --- oscillation length $L_{\mathrm{mix}}$};

% --- Spectral group (blue) ---
\node[mtype=BlueLine, below=0.35cm of m5] (m6)
    {\textbf{spectrum} --- $|\hat{\varphi}(k)|^2$ power};
\node[mtype=BlueLine, below=of m6] (m7)
    {\textbf{spectral\_conversion} --- per-mode $P(k)$};
\node[mtype=BlueLine, below=of m7] (m8)
    {\textbf{dispersion} --- $\omega(k)$ relation};

% --- Derived group (pink) ---
\node[mtype=PinkLine, below=0.35cm of m8] (m9)
    {\textbf{effective\_mass} --- $m_{\mathrm{eff}}^2$ from $\omega''(k)$};
\node[mtype=PinkLine, below=of m9] (m10)
    {\textbf{velocity} --- group/phase velocity};
\node[mtype=PinkLine, below=of m10] (m11)
    {\textbf{resonance} --- Lorentzian fit};

% --- Other (gray) ---
\node[mtype=GrayLine, below=0.35cm of m11] (m12)
    {\textbf{asymptotic} --- power-law tail fitting};
\node[mtype=GrayLine, below=of m12] (m13)
    {\textbf{summary} --- global statistics};

% Fan-out arrows
\foreach \m in {m1,m2,m3,m4,m5,m6,m7,m8,m9,m10,m11,m12,m13} {
    \draw[arr] (disp.east) -- (\m.west);
}

% Output
\node[output, right=1.2cm of m7] (out) {scalar metrics\\[1pt]\scriptsize CSV / JSON};
\foreach \m in {m1,m2,m3,m4,m5,m6,m7,m8,m9,m10,m11,m12,m13} {
    \draw[arr] (\m.east) -- (out.west |- \m.east);
}

% Group labels
\node[font=\scriptsize\bfseries, text=GreenLine,
      left=0.1cm of m1.north west, anchor=south east] {Energy};
\node[font=\scriptsize\bfseries, text=PurpleLine,
      left=0.1cm of m3.north west, anchor=south east] {Conversion};
\node[font=\scriptsize\bfseries, text=BlueLine,
      left=0.1cm of m6.north west, anchor=south east] {Spectral};
\node[font=\scriptsize\bfseries, text=PinkLine,
      left=0.1cm of m9.north west, anchor=south east] {Derived};

\end{tikzpicture}
\end{document}
