\documentclass[border=10pt]{standalone}
\usepackage{tidal-tikz-styles}

\begin{document}
\begin{tikzpicture}[
    node distance=0.6cm and 1.2cm
]

% Row 1: Config
\node[stage=Gray] (config)
    {\textbf{Sweep configuration}\\[1pt]
     \scriptsize parameter range specification};

% Row 2: Grid generation
\node[stage=Pink, below=0.7cm of config] (grid)
    {\textbf{Parameter grid}\\[1pt]
     \scriptsize grid / Latin hypercube / Sobol};
\draw[arr] (config) -- (grid);

% Row 3: Dispatch
\node[stage=Purple, below=0.7cm of grid, minimum width=3.0cm] (dispatch)
    {\textbf{Dispatch workers}\\[1pt]
     \scriptsize sequential or parallel};
\draw[arr] (grid) -- (dispatch);

% Row 4: Per-point pipeline (boxed)
\node[stage=Blue, below left=0.9cm and 0.3cm of dispatch,
      minimum width=2.0cm] (sim) {Simulate};
\node[stage=Green, right=0.8cm of sim,
      minimum width=2.0cm] (meas) {Measure};
\draw[arr] (sim) -- (meas);

% Box around per-point
\node[fit=(sim)(meas), draw=GrayLine, dashed, rounded corners=3pt,
      inner sep=8pt, label={[font=\scriptsize\itshape, text=GrayLine]
      above:per parameter point}] (perpoint) {};

\draw[arr] (dispatch) -- (perpoint);

% Row 5: Status tracking
\node[stage=Orange, right=2.0cm of dispatch, minimum width=2.2cm] (status)
    {\textbf{Status tracking}\\[1pt]
     \scriptsize pending $\to$ running\\
     \scriptsize $\to$ complete / error};
\draw[arr, densely dotted, OrangeLine] (dispatch) -- (status);

% Resume
\node[annot, below=0.15cm of status] {Resumable:\\skips completed};

% Row 6: Aggregate
\node[stage=Lime, below=0.9cm of perpoint] (agg)
    {\textbf{Aggregate results}};
\draw[arr] (perpoint) -- (agg);

% Row 7: Outputs
\node[stage=Gray,
      below left=0.8cm and 1.0cm of agg] (csv)
    {Tabulated results};
\node[stage=Lime, below=0.8cm of agg] (plot)
    {\textbf{Visualisation}};
\node[stage=Lime, below right=0.8cm and 1.0cm of agg] (analyze)
    {\textbf{Sensitivity analysis}\\[1pt]
     \scriptsize Sobol / Morris};

\draw[arr] (agg) -- (csv);
\draw[arr] (agg) -- (plot);
\draw[arr] (agg) -- (analyze);

% Plot types
\node[annot, below=0.15cm of plot, text width=3.0cm]
    {1D: line plot\\2D: heatmap\\multi-D: tornado};

\end{tikzpicture}
\end{document}
