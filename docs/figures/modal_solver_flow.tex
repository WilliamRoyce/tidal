\documentclass[border=10pt]{standalone}
\usepackage{tidal-tikz-styles}
\usetikzlibrary{backgrounds}

\begin{document}
\begin{tikzpicture}[
    node distance=0.8cm
]

% ===================================================================
% ELIGIBILITY GATE (top)
% ===================================================================
\node[check, text width=3.2cm] (gate)
    {flat metric $\cdot$ periodic BCs\\
     supported operators\\
     time-independent coeffs\\
     constraints eliminable};

% ===================================================================
% PHYSICAL SPACE INPUT
% ===================================================================
\node[stage=Gray,
      left=0.8cm of gate.west] (input)
    {$\mathbf{y}(x, t_0)$\\[-1pt]\scriptsize physical space};


% ===================================================================
% FFT
% ===================================================================
\node[stage=Purple,
      below=0.4cm of input] (fft)
    {\textbf{FFT}\\[-1pt]\scriptsize per field slot};

% ===================================================================
% DECISION: Position-dependent coefficients?
% ===================================================================
\node[decision, below=0.4cm of fft] (branch)
    {Position-dep.\\coefficients?};

% ===================================================================
% PATH D: Per-mode Padé matrix-exp (upper branch, pink)
% ===================================================================
\node[stage=Pink,
      below right=1.0cm and 0.6cm of branch.east, anchor=north] (matrix_pm)
    {Build $M = B^{-1}A(k)$\\[-1pt]\scriptsize independent per mode $k$};

\node[stage=Pink,
      below=0.4cm of matrix_pm] (pade)
    {Precompute $\exp(M \cdot dt)$\\[-1pt]\scriptsize via \texttt{scipy.linalg.expm} (Pad\'{e})\\[-1pt]\scriptsize once per mode};

\node[stage=Pink,
      below=0.4cm of pade] (evolve_pade)
    {Snapshot loop\\[-1pt]\scriptsize $\hat{y}_{n+1} = \exp(M\,dt)\cdot\hat{y}_n$\\[-1pt]\scriptsize $O(bs^2)$ matvec per mode};

% ===================================================================
% PATH B: expm_multiply (lower branch, blue)
% ===================================================================
\node[stage=Blue,
      below left=1.0cm and 0.6cm of branch.west, anchor=north] (matrix_full)
    {Build full matrix\\[-1pt]\scriptsize $c(x)\!\cdot\!u \to \hat{c}\ast\hat{u}$ couples all modes};

\node[stage=Blue,
      below=0.4cm of matrix_full] (sparsify)
    {Sparsify\\[-1pt]\scriptsize zero negligible entries; sparse CSC};

\node[stage=Blue,
      below=0.4cm of sparsify] (expm)
    {Matrix exponential\\[-1pt]\scriptsize $\exp(A \cdot \Delta t) \cdot y_0$ (all $\Delta t$ at once)};

% ===================================================================
% CONVERGENCE: IFFT + output
% ===================================================================
\node[stage=Purple,
      below=3.8cm of branch] (ifft)
    {\textbf{IFFT}\\[-1pt]\scriptsize per field slot};

\node[stage=Gray,
      below=0.4cm of ifft] (output)
    {$\mathbf{y}(x, t)$\\[-1pt]\scriptsize physical space};

% ===================================================================
% ARROWS — main flow
% ===================================================================
\draw[arr, densely dotted, OrangeLine] (gate.west) -- (input.east);
\draw[arr] (input) -- (fft);
\draw[arr] (fft) -- (branch);

% Path D (upper): No position-dependent → per-mode Padé expm
\draw[arr] (branch.east) -| node[yeslbl, below left] {No} (matrix_pm.north);
\draw[arr] (matrix_pm) -- (pade);
\draw[arr] (pade) -- (evolve_pade);
\draw[arr, rounded corners=3pt]
    (evolve_pade.south) |- (ifft.east);

% Path B (lower): Yes position-dependent → sparsify → expm_multiply
\draw[arr] (branch.west) -| node[nolbl, below right] {Yes} (matrix_full.north);
\draw[arr] (matrix_full) -- (sparsify);
\draw[arr] (sparsify) -- (expm);
\draw[arr, rounded corners=3pt]
    (expm.south) |- (ifft.west);

% IFFT -> output
\draw[arr] (ifft) -- (output);


% ===================================================================
% BLOCK-AWARE + SPARSE-IC CHECK (above per-mode matrix, Path D only)
% ===================================================================
\node[check, above =1.2cm of matrix_pm.north east, text width=4.6cm] (blocks)
    {Detect decoupled field blocks\\[1pt]
     \textbf{sparse-IC}: skip modes with $|\hat{y}_0|=0$ entirely\\[1pt]
     \textbf{eigenvalue pre-check}: $\mathrm{Re}(\lambda_{\max})>0$\\
     via \texttt{np.linalg.eigvals} (no full decomp)\\[1pt]
     \scriptsize (eigenvalue pre-check replaces per-mode expm pre-check)};
\draw[arr, densely dotted, OrangeLine] ([xshift=-0.8cm]blocks.south) -- ([xshift=-0.8cm]matrix_pm.north east);

% ===================================================================
% CONSTRAINT ELIMINATION (Schur complement, Path D only)
% ===================================================================
\node[detail=Pink, text width=3.1cm, below right=0.6cm and 0.4cm of matrix_pm.east, anchor=west] (schur)
    {\textbf{Schur complement}\\[2pt]
     eliminate constraints:\\
     $A_{\mathrm{red}} = A_{dd} - A_{dc} S_{cc}^{-1} S_{cd}$\\[2pt]
     (dynamical DOFs only)};

\node[annot, above=0.0cm of schur] {if constraints present};

% Arrow from matrix_pm down to Schur, then to pade
\draw[arr, PinkLine, densely dashed, rounded corners=3pt]
    (matrix_pm.east) -- ([yshift=0.6cm]schur.west);
\draw[arr, PinkLine, densely dashed, rounded corners=3pt]
    ([yshift=-0.63cm]schur.west) -- (pade.east);

% Tachyonic note (right of evolve_pade node)
\node[detail=Orange, text width=3.4cm, right=0.5cm of evolve_pade, anchor=west] (tachyon_note)
    {\textbf{Tachyonic modes}\\[2pt]
     Arises in e.g.\ PGT with $\tilde{R}^2$:\\
     wrong-sign mass eigenvalues in\\
     unphysical (axial/tensor) sector.\\[2pt]
     \textbf{Pre-check}: $\mathrm{Re}(\lambda)>0$\\
     detected via \texttt{eigvals}\\
     (no full decomp needed).\\[2pt]
     \textbf{Primary guard}: sparse-IC\\
     skips modes with $|\hat{y}_0|=0$,\\
     so noise never amplifies.};

\draw[arr, OrangeLine, densely dashed, rounded corners=3pt]
    (evolve_pade.east) -- (tachyon_note.west);

% Recovery arrow from evolve_pade down to reconstruction
\node[detail=Pink, text width=3.0cm, below right=0.2cm and -0.6cm of evolve_pade, text width=2.8cm] (recover)
    {\textbf{Reconstruct}\\[2pt]
     $\hat{c}(k) = -S_{cc}^{-1} S_{cd}\, \hat{d}(k)$\\[2pt]
     IFFT $\to c(x,t)$};
\draw[arr, PinkLine, densely dashed, rounded corners=3pt]
    (evolve_pade.east) -| (recover.north);
\draw[arr, PinkLine, densely dashed, rounded corners=3pt]
    ([yshift=-0.5cm]recover.west) -- ([yshift=-0.2cm]ifft.east);

% ===================================================================
% PASS 1 FOOTNOTE (v6 perturbative)
% ===================================================================
\node[detail=Gray, text width=4.8cm,
      below=0.4cm of recover.south, anchor=north] (pass1)
    {\textbf{Eigendecomposition: opt-in only}\\[2pt]
     \texttt{collect\_eigendata} flag activates\\
     per-block $M$, $y_0$ collection for\\
     Pass~1 perturbative-Duhamel callers\\
     (\texttt{\_evolve\_duhamel\_per\_mode}).\\[2pt]
     Pass~1 uses augmented matrix-exp\\
     $\exp\!\bigl(t\cdot\bigl[\begin{smallmatrix}A&S\\0&A\end{smallmatrix}\bigr]\bigr)$
     (Al-Mohy \& Higham 2011~\S5.2).\\[2pt]
     \scriptsize v6 perturbative architecture only};

% ===================================================================
% BACKGROUND ZONES
% ===================================================================
\begin{scope}[on background layer]
    \draw[white] (input) --++ (8,0);
    \draw[white] (input) --++ (-8,0);
    % Physical space zone (blue tint)
    \node[fit=(input)(output), inner sep=12pt, fill=GrayFill, densely dashed, draw=GrayLine, fill opacity=0.2, rounded corners=5pt] {};
    % Fourier space zone (purple tint) — encompasses both branches
    \node[fit=(fft)(ifft)(matrix_pm)(pade)(evolve_pade)(matrix_full)(sparsify)(expm),
          inner sep=4pt, fill=PurpleFill, draw=PurpleLine, densely dashed,
          fill opacity=0.08, rounded corners=5pt] {};
\end{scope}

% Zone labels
\node[font=\scriptsize\bfseries, text=GrayLine,
      above left=0.0cm and -0.4cm of input.north, anchor=south east]
    {Physical space};
\node[font=\scriptsize\bfseries, text=PurpleLine,
      above left=0.2cm and 0.8cm of fft.west, anchor=east]
    {Fourier space};

% ===================================================================
% PATH LEGEND (bottom right)
% ===================================================================
% \node[font=\scriptsize, align=left,
%       below right=0.2cm and 0.8cm of output.east, anchor=west,
%       draw=GrayLine, rounded corners=2pt, inner sep=5pt, fill=white] {
%     \textcolor{PinkLine}{\rule{0.5cm}{2pt}}
%         \textbf{Path D}: per-mode Pad\'{e} expm (constant coeffs)\\
%     \textcolor{BlueLine}{\rule{0.5cm}{2pt}}
%         \textbf{Path B}: sparse \texttt{expm\_multiply} (position-dep.)\\
%     \textcolor{PinkLine}{\rule[1pt]{0.5cm}{1pt}}
%         Schur constraint elimination (Path D only)
% };

\end{tikzpicture}
\end{document}
