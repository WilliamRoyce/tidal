\begin{frame}{Pre-campaign results}
  \small
  Three models studied to determine the structure of the systematic HPC scan.

  \vspace{0.5em}
  \textbf{1. Minimal PGT (Einstein--Cartan + $\alpha_i I_i + b_5\Rt^2$).}\\
  Two algebraically decoupled channels.  The
  Gertsenshtein channel contains
  \emph{no torsion fields} coupling: $A=1$ exactly, non-perturbatively in $b_5$.

  \vspace{0.4em}
  \textbf{2. Plasma baseline (Einstein--Maxwell + $m_A^2$).}\\
  Effective photon mass detunes the resonance: $P_{\max}$ monotonically
  suppressed by $m_A^2$, which dominates the parameter response in pure GR.

  \vspace{0.4em}
  \textbf{3. Dark-photon BSM analogy} $\mathcal{L} = \frac{1}{\kappa^2} R - \tfrac{1}{4}F^2 - \tfrac{1}{4}\xi F_T^2 + \delta_m F\cdot F_T - \alpha_3 I_3$.\\
  Torsion as a hidden BSM vector with mass from the $I_3$ invariant.\\
  \begin{itemize}
    \item \emph{Vacuum}: Photon and dark photon exact eigenmodes --- no conversion channel
    \item \emph{Plasma added} as the $\omega_p^2$ effective photon mass breaks the eigenmode
    \item \emph{Result}: $\delta_m$ has \emph{negligible} effect;
    $m_A^2$ dominates only as Gertsenshtein detuning.
  \end{itemize}
  
\end{frame}
